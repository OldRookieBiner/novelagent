# v0.8.1 代码优化与架构重构 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 v0.8.0 审计发现的严重 Bug 和架构问题，统一流式 SSE 处理、移除重复代码、将持久化逻辑移入 LangGraph 节点、优化性能

**Architecture:** 分四个阶段实施。Phase 1 修复 3 个紧急问题（SSE 流错误处理、confirm_workflow 持久化缺失、大纲重复 LLM 调用）。Phase 2 提取公共 SSE 流式工具并统一 run/confirm 的 stream 实现。Phase 3 消除节点内冗余 DB 会话、优化查询。Phase 4 代码质量修复

**Tech Stack:** FastAPI + LangGraph + SQLAlchemy + SSE (与项目一致)

**Spec:** `docs/superpowers/specs/2026-05-05-v0.8.1-optimization-design.md`

---

### Task 1: 修复 SSE 流中 HTTPException 异常关闭连接

**Files:**
- Modify: `backend/app/api/chapters.py:708-730`

- [ ] **Step 1: 将 SSE 生成器中的 raise HTTPException 替换为 yield error 事件**

将第 708-712 行的 `raise HTTPException` 替换为 SSE error 事件：

```python
# 修改前 (chapters.py:708-712)
content = clean_chapter_content(accumulated_content) if accumulated_content else ""
if not content:
    raise HTTPException(
        status_code=status.HTTP_500_INTERNAL_SERVER_ERROR,
        detail="AI 返回内容为空，请重试",
    )

# 修改后
content = clean_chapter_content(accumulated_content) if accumulated_content else ""
if not content:
    yield format_sse_error(ValueError("AI 返回内容为空，请重试"))
    return
```

- [ ] **Step 2: 同样需要移除 except HTTPException 的捕获**

第 728 行的 `except HTTPException: raise` 分支需移除，因为不再抛出 HTTPException：

```python
# 修改前 (chapters.py:728-731)
        except HTTPException:
            raise
        except Exception as e:
            yield format_sse_error(e)

# 修改后
        except Exception as e:
            yield format_sse_error(e)
```

同时移除顶部已不再需要的 HTTPException 导入（如果不再被其他代码使用，检查后确定）。

- [ ] **Step 3: 运行章节生成测试验证**

```bash
docker exec novelagent-backend-1 pytest tests/ -v -k "chapter" 2>&1 | tail -20
```

- [ ] **Step 4: Commit**

```bash
git add backend/app/api/chapters.py
git commit -m "fix(api): replace HTTPException with SSE error event in chapter generation stream

SSE generator functions use yield format_sse_error() instead of raise
HTTPException which caused unclean connection teardown. Fixes B1."
```

---

### Task 2: 提取持久化逻辑为独立函数

**Files:**
- Create: `backend/app/utils/workflow_persistence.py`
- Modify: `backend/app/api/workflow.py`

- [ ] **Step 1: 创建持久化工具模块**

创建 `backend/app/utils/workflow_persistence.py`：

```python
"""工作流节点输出持久化工具

将 LangGraph 节点的输出结果写入数据库。
在 run_workflow 和 confirm_workflow 的 SSE 流式处理中复用。
"""

import logging
from sqlalchemy.orm import Session

from app.models.outline import Outline, ChapterOutline
from app.models.chapter import Chapter
from app.agents.nodes.review import check_review_passed

logger = logging.getLogger(__name__)


def persist_outline(output: dict, project_id: int, outline: Outline, db: Session):
    """持久化大纲生成节点的输出到 outlines 表"""
    new_title = output.get("outline_title", "")
    new_summary = output.get("outline_summary", "")
    new_characters = output.get("outline_characters", [])
    new_plot_points = output.get("outline_plot_points", [])

    if not new_title and not new_summary and not new_characters and not new_plot_points:
        logger.warning(
            f"persist_outline: empty data for project {project_id}"
        )
        return False

    if new_title:
        outline.title = new_title
    if new_summary:
        outline.summary = new_summary
    if new_plot_points:
        outline.plot_points = new_plot_points
    if new_characters:
        outline.characters = new_characters

    outline.world_setting = output.get(
        "outline_world_setting", outline.world_setting or {}
    )
    outline.emotional_curve = output.get(
        "outline_emotional_curve", outline.emotional_curve
    )
    outline.chapter_count_suggested = output.get(
        "chapter_count", outline.chapter_count_suggested
    )

    logger.info(
        f"persist_outline: project {project_id}: "
        f"title='{new_title}', char={len(new_characters)}, "
        f"plot={len(new_plot_points)}"
    )
    return True


def persist_chapter_content(output: dict, project_id: int, db: Session):
    """持久化章节内容生成节点的输出到 chapters 表"""
    written_chapters = output.get("written_chapters", [])
    for chapter_data in written_chapters:
        chapter_num = chapter_data.get("chapter_number")
        if not chapter_num:
            continue
        chapter_outline = (
            db.query(ChapterOutline)
            .filter(
                ChapterOutline.project_id == project_id,
                ChapterOutline.chapter_number == chapter_num,
            )
            .first()
        )
        if not chapter_outline:
            continue
        chapter = (
            db.query(Chapter)
            .filter(Chapter.chapter_outline_id == chapter_outline.id)
            .first()
        )
        if not chapter:
            chapter = Chapter(
                chapter_outline_id=chapter_outline.id,
                content=chapter_data.get("content", ""),
                word_count=chapter_data.get("word_count", 0),
                review_passed=False,
                review_feedback=None,
            )
            db.add(chapter)
        else:
            chapter.content = chapter_data.get("content", chapter.content)
            chapter.word_count = chapter_data.get(
                "word_count", chapter.word_count
            )
    logger.info(
        f"persist_chapter_content: project {project_id}"
    )


def persist_review_result(output: dict, project_id: int, db: Session):
    """持久化审核节点的输出到 chapters 表"""
    review_result = output.get("review_result", {})
    current_chapter = output.get("current_chapter", 1)
    reviewed_chapter_num = current_chapter - 1
    chapter_outline = (
        db.query(ChapterOutline)
        .filter(
            ChapterOutline.project_id == project_id,
            ChapterOutline.chapter_number == reviewed_chapter_num,
        )
        .first()
    )
    if chapter_outline:
        chapter = (
            db.query(Chapter)
            .filter(Chapter.chapter_outline_id == chapter_outline.id)
            .first()
        )
        if chapter:
            chapter.review_passed = check_review_passed(review_result)
            chapter.review_feedback = review_result.get("raw_response")
            chapter.review_result = review_result
    logger.info(
        f"persist_review_result: project {project_id}"
    )


def persist_rewrite_result(output: dict, project_id: int, db: Session):
    """持久化重写节点的输出到 chapters 表"""
    written_chapters = output.get("written_chapters", [])
    for chapter_data in written_chapters:
        chapter_num = chapter_data.get("chapter_number")
        if not chapter_num:
            continue
        chapter_outline = (
            db.query(ChapterOutline)
            .filter(
                ChapterOutline.project_id == project_id,
                ChapterOutline.chapter_number == chapter_num,
            )
            .first()
        )
        if not chapter_outline:
            continue
        chapter = (
            db.query(Chapter)
            .filter(Chapter.chapter_outline_id == chapter_outline.id)
            .first()
        )
        if chapter:
            chapter.content = chapter_data.get("content", chapter.content)
            chapter.word_count = chapter_data.get(
                "word_count", chapter.word_count
            )
            chapter.rewrite_count = output.get(
                "rewrite_count", chapter.rewrite_count
            )
    logger.info(
        f"persist_rewrite_result: project {project_id}"
    )


# 节点名到持久化函数的映射
NODE_PERSIST_MAP = {
    "outline_generation_node": persist_outline,
    "generate_chapter_content_node": persist_chapter_content,
    "review_node": persist_review_result,
    "rewrite_node": persist_rewrite_result,
}
```

- [ ] **Step 2: 修改 run_workflow 的 on_chain_end 处理**

在 `workflow.py` 的 `run_workflow` 函数中，修改 `stream_generator()` 的 `on_chain_end` 分支：

```python
# 修改前 (workflow.py:284-425) - 4 段独立的持久化代码
# 修改后 - 替换为统一调用

                elif event_type == "on_chain_end":
                    # 节点执行完成
                    node_name = event_name
                    output = event_data.get("output", {})

                    if isinstance(output, dict):
                        yield f"event: node_done\ndata: {json.dumps({'node': node_name, 'state': output})}\n\n"

                    # 统一的持久化分发
                    if node_name in NODE_PERSIST_MAP:
                        persist_fn = NODE_PERSIST_MAP[node_name]
                        if node_name == "outline_generation_node":
                            if not output.get("outline_title") and not output.get("outline_summary") and not output.get("outline_characters") and not output.get("outline_plot_points"):
                                yield format_sse_error(ValueError("大纲生成失败，AI 返回数据为空，请重试"))
                                return
                            else:
                                persist_fn(output, project_id, outline, db)
                        else:
                            persist_fn(output, project_id, db)
                        db.commit()

                    # 关系生成节点完成后自动确认大纲
                    if node_name == "generate_relations_node":
                        outline.confirmed = True
                        outline.chapter_count_confirmed = True
                        db.commit()
                        logger.info(
                            f"workflow: auto-confirmed outline for project {project_id}"
                        )
                        yield f"event: done\ndata: {json.dumps({'message': 'Generation completed'})}\n\n"
                        return
```

移除 workflow.py 中原有的 4 段独立持久化代码（第 292-425 行）。

- [ ] **Step 3: 修改 confirm_workflow 添加持久化支持**

在 `confirm_workflow` 中，对 `stream_generator()` 的 `on_chain_end` 添加持久化调用：

```python
# 在 confirm_workflow 的 stream_generator() 中
# 将现有的简单 on_chain_end 处理替换为持久化版本

# 需要在 confirm_workflow 函数开头获取 outline:
outline = db.query(Outline).filter(
    Outline.project_id == project_id
).first()

# on_chain_end 处理改为:
elif event_type == "on_chain_end":
    node_name = event_name
    output = event_data.get("output", {})
    if isinstance(output, dict):
        yield f"event: node_done\ndata: {json.dumps({'node': node_name, 'state': output})}\n\n"

    # 统一的持久化分发（与 run_workflow 相同）
    if node_name in NODE_PERSIST_MAP:
        persist_fn = NODE_PERSIST_MAP[node_name]
        if node_name == "outline_generation_node":
            if outline and not output.get("outline_title") and not output.get("outline_summary") and not output.get("outline_characters") and not output.get("outline_plot_points"):
                yield format_sse_error(ValueError("大纲生成失败，AI 返回数据为空，请重试"))
                return
            else:
                persist_fn(output, project_id, outline, db)
        else:
            persist_fn(output, project_id, db)
        db.commit()
```

- [ ] **Step 4: 添加导入**

在 `workflow.py` 顶部添加导入：

```python
from app.utils.workflow_persistence import (
    NODE_PERSIST_MAP,
    persist_outline,
    persist_chapter_content,
    persist_review_result,
    persist_rewrite_result,
)
from app.models.outline import Outline
```

- [ ] **Step 5: 运行全量后端测试**

```bash
docker exec novelagent-backend-1 pytest -v 2>&1 | tail -30
```

确保所有测试通过。

- [ ] **Step 6: Commit**

```bash
git add backend/app/utils/workflow_persistence.py backend/app/api/workflow.py
git commit -m "refactor(workflow): extract persistence logic into shared module

Extract 4 persistence blocks from run_workflow stream_generator into
workflow_persistence.py. Add persistence support to confirm_workflow.
Fixes B2 (data loss on workflow resume after confirmation)."
```

---

### Task 3: 修复大纲生成重复 LLM 调用

**Files:**
- Modify: `backend/app/api/outline.py:132-219`

- [ ] **Step 1: 修改 generate_outline 路由，从 output 获取解析结果**

将 `stream_generator()` 中二次累加 + 二次 parse 的逻辑替换为从 `stream_node_events` 的 done 事件中获取 `output`：

```python
# 修改 outline.py 中的 stream_generator()

async def stream_generator():
    """Generate outline via LangGraph and stream via SSE."""
    try:
        async for sse_event in stream_node_events(graph, graph_state, config):
            yield sse_event

        # 从 graph ainvoke 获取结果（不再二次 parse）
        # stream_node_events 完成后，通过 ainvoke 获取最终状态
        result_state = await graph.ainvoke(graph_state, config)
        parsed: dict[str, Any] = {
            "title": result_state.get("outline_title", ""),
            "summary": result_state.get("outline_summary", ""),
            "plot_points": result_state.get("outline_plot_points", []),
            "characters": result_state.get("outline_characters", []),
            "world_setting": result_state.get("outline_world_setting", {}),
            "emotional_curve": result_state.get("outline_emotional_curve"),
        }

        logger.info(
            f"outline from state: title='{parsed.get('title', '')}', "
            f"content_len={len(parsed.get('summary', ''))}"
        )

        if parsed.get("title"):
            outline.title = parsed["title"]
            outline.summary = parsed["summary"]
            outline.plot_points = parsed["plot_points"]
            outline.characters = parsed.get("characters", [])
            outline.world_setting = parsed.get("world_setting", {})
            outline.emotional_curve = parsed.get("emotional_curve")
            logger.info(f"outline updated: title='{parsed.get('title', '')}'")

        # 更新工作流状态
        workflow_state = get_or_create_workflow_state(db, project_id)
        workflow_state.stage = STAGE_OUTLINE

        db.commit()
        db.refresh(outline)

        # Send completion event with parsed outline
        completion_data = {
            "outline": {
                "title": parsed["title"],
                "summary": parsed["summary"],
                "plot_points": parsed["plot_points"],
                "characters": parsed.get("characters", []),
                "world_setting": parsed.get("world_setting", {}),
                "emotional_curve": parsed.get("emotional_curve"),
                "confirmed": False,
                "chapter_count_suggested": outline.chapter_count_suggested,
            },
            "stage": STAGE_OUTLINE,
        }
        yield f"event: done\ndata: {json.dumps(completion_data)}\n\n"

    except Exception as e:
        yield format_sse_error(e)
```

移除不再需要的 `accumulated_content` 变量和 `parse_outline` 导入。
移除 `stream_generator` 中无用的 `event_count`、`data_event_count` 计数器。

- [ ] **Step 2: 运行大纲生成测试**

```bash
docker exec novelagent-backend-1 pytest tests/ -v -k "outline" 2>&1 | tail -20
```

- [ ] **Step 3: Commit**

```bash
git add backend/app/api/outline.py
git commit -m "fix(api): remove duplicate LLM call in outline generation

Use state from ainvoke result instead of accumulating chunks and
re-parsing. The outline_generation_node already calls parse_outline
internally. Fixes P1 (double LLM invocation)."
```

---

### Task 4: 创建统一 SSE 流式工具

**Files:**
- Create: `backend/app/agents/sse_events.py`
- Modify: `backend/app/api/workflow.py`
- Modify: `backend/app/api/chapters.py`

- [ ] **Step 1: 创建 SSE 事件格式化工具**

创建 `backend/app/agents/sse_events.py`：

```python
"""统一的 SSE 事件格式化工具"""

import json
from typing import Any


def format_node_start(node_name: str, message: str = "Starting") -> str:
    """格式化节点开始 SSE 事件"""
    return (
        f"event: node_start\n"
        f"data: {json.dumps({'node': node_name, 'message': message})}\n\n"
    )


def format_node_done(node_name: str, output: dict[str, Any]) -> str:
    """格式化节点完成 SSE 事件"""
    return (
        f"event: node_done\n"
        f"data: {json.dumps({'node': node_name, 'state': output})}\n\n"
    )


def format_chunk(content: str) -> str:
    """格式化内容块 SSE 事件"""
    return f"event: chunk\ndata: {json.dumps({'content': content})}\n\n"


def format_done(message: str = "Workflow completed", extra: dict[str, Any] | None = None) -> str:
    """格式化完成 SSE 事件"""
    data: dict[str, Any] = {"message": message}
    if extra:
        data.update(extra)
    return f"event: done\ndata: {json.dumps(data)}\n\n"


def format_waiting(confirmation_type: str) -> str:
    """格式化等待确认 SSE 事件"""
    return (
        f"event: waiting\n"
        f"data: {json.dumps({'confirmation_type': confirmation_type})}\n\n"
    )


def format_error(message: str) -> str:
    """格式化错误 SSE 事件"""
    return f"event: error\ndata: {json.dumps({'error': message})}\n\n"


def extract_chunk_from_event(event_data: dict[str, Any]) -> str | None:
    """从 LangGraph astream_events 的 on_chat_model_stream 事件中提取内容"""
    chunk = event_data.get("chunk")
    if chunk:
        content = getattr(chunk, "content", str(chunk))
        if content:
            return content
    return None
```

- [ ] **Step 2: 更新 workflow.py run_workflow 使用 SSE 工具**

将 workflow.py 中手写的 `f"event: node_start\ndata: ..."` 字符串替换为工具函数调用：

```python
# 替换所有手写 SSE 格式
# 例如:
# yield f"event: node_start\ndata: {json.dumps({'node': node_name})}\n\n"
# 改为:
# yield format_node_start(node_name)
```

- [ ] **Step 3: 更新 chapters.py 使用 SSE 工具**

同样将 `chapters.py` 中的手写 SSE 格式替换。

- [ ] **Step 4: 在 stream_node_events 中使用 SSE 工具**

更新 `backend/app/agents/streaming.py`，使用 `sse_events.py` 中的函数。

- [ ] **Step 5: 运行测试验证**

```bash
docker exec novelagent-backend-1 pytest -v 2>&1 | tail -20
```

- [ ] **Step 6: Commit**

```bash
git add backend/app/agents/sse_events.py backend/app/api/workflow.py \
        backend/app/api/chapters.py backend/app/agents/streaming.py
git commit -m "refactor(sse): extract SSE event formatting into shared module

Create sse_events.py with format_node_start/format_node_done/format_chunk
etc. Replace inline SSE string formatting across workflow.py, chapters.py,
and streaming.py. Part of Q1 (eliminate stream_generator duplication)."
```

---

### Task 5: 修复 B3 - confirm_chapter_outline 竞态条件

**Files:**
- Modify: `backend/app/api/chapters.py:344-364`

- [ ] **Step 1: 将计数逻辑改为 commit 之后重新查询**

```python
# 修改前 (chapters.py:344-364)
    # Check if all chapter outlines are confirmed - 使用两个简单查询
    from sqlalchemy import func

    total_outlines = db.query(func.count(ChapterOutline.id)).filter(
        ChapterOutline.project_id == project_id
    ).scalar() or 0

    confirmed_outlines = db.query(func.count(ChapterOutline.id)).filter(
        ChapterOutline.project_id == project_id,
        ChapterOutline.confirmed == True
    ).scalar() or 0

    # 确认后，已确认数需要 +1（因为当前章节还未 commit）
    confirmed_outlines += 1

    # If all confirmed, update workflow state to chapter writing
    if total_outlines > 0 and confirmed_outlines == total_outlines:
        workflow_state = get_or_create_workflow_state(db, project_id)
        workflow_state.stage = STAGE_WRITING

    db.commit()

# 修改后
    # 先 commit 当前确认
    db.commit()

    # commit 后重新查询计数，避免竞态条件
    from sqlalchemy import func

    total_outlines = db.query(func.count(ChapterOutline.id)).filter(
        ChapterOutline.project_id == project_id
    ).scalar() or 0

    confirmed_outlines = db.query(func.count(ChapterOutline.id)).filter(
        ChapterOutline.project_id == project_id,
        ChapterOutline.confirmed == True
    ).scalar() or 0

    # If all confirmed, update workflow state to chapter writing
    if total_outlines > 0 and confirmed_outlines >= total_outlines:
        workflow_state = get_or_create_workflow_state(db, project_id)
        workflow_state.stage = STAGE_WRITING
        db.commit()
```

注意：需要移除函数末尾原有的 `db.commit()` 调用，因为已经在上面 commit 过了。同时需要在分支外保留对 `db.refresh(chapter_outline)` 的调用。

- [ ] **Step 2: 运行章节相关测试**

```bash
docker exec novelagent-backend-1 pytest tests/ -v -k "chapter" 2>&1 | tail -20
```

- [ ] **Step 3: Commit**

```bash
git add backend/app/api/chapters.py
git commit -m "fix(api): fix race condition in confirm_chapter_outline count

Commit before re-querying confirmed count to avoid manual +1
off-by-one. Use >= comparison for tolerance. Fixes B3."
```

---

### Task 6: 修复 B4 - generate_relations 返回数据库 id

**Files:**
- Modify: `backend/app/api/characters.py:850-918`

- [ ] **Step 1: 从 write_relations_to_db 返回值获取带 id 的数据**

```python
# 修改前 (characters.py:908-917)
    graph = create_single_node_graph(generate_relations_node)
    config = {"configurable": {"thread_id": f"relations-{project_id}"}}

    result_state = await graph.ainvoke(graph_state, config)

    relations = result_state.get("relations", [])

    return {
        "message": f"Created {len(relations)} relations",
        "relations": relations,
    }

# 修改后
    graph = create_single_node_graph(generate_relations_node)
    config = {"configurable": {"thread_id": f"relations-{project_id}"}}

    result_state = await graph.ainvoke(graph_state, config)

    # relations 来自节点返回的 state，包含 parse 结果（无 db id）
    # 需要从数据库重新查询获取带 id 的完整数据
    from app.models.character import Relation
    db_relations = (
        db.query(Relation)
        .filter(Relation.project_id == project_id)
        .all()
    )

    return {
        "message": f"Created {len(db_relations)} relations",
        "relations": [
            {
                "id": r.id,
                "character_a_id": r.character_a_id,
                "character_b_id": r.character_b_id,
                "relation_type": r.relation_type,
                "trust_level": r.trust_level,
                "current_status": r.current_status,
                "direction": r.direction,
            }
            for r in db_relations
        ],
    }
```

- [ ] **Step 2: 运行关系相关测试**

```bash
docker exec novelagent-backend-1 pytest tests/ -v -k "relation" 2>&1 | tail -20
```

- [ ] **Step 3: Commit**

```bash
git add backend/app/api/characters.py
git commit -m "fix(api): return database ids in generate_relations response

Query Relation table after generation to get records with proper ids
instead of returning parse results without ids. Fixes B4."
```

---

### Task 7: 修复 B6 - 将 create_characters_from_outline_node 改为 async

**Files:**
- Modify: `backend/app/agents/nodes/character_generation.py:78-100`

- [ ] **Step 1: 改为 async 函数，DB 操作放入线程池**

```python
# 修改前
def create_characters_from_outline_node(state: NovelState) -> NovelState:

# 修改后
async def create_characters_from_outline_node(state: NovelState) -> NovelState:
    """LangGraph 节点：从大纲提取角色写入数据库

    签名： (state: NovelState) -> NovelState

    异步节点，DB 操作放入线程池避免阻塞 event loop。
    """
    import asyncio
    loop = asyncio.get_event_loop()
    characters = await loop.run_in_executor(
        None, extract_characters_from_outline, state
    )

    import logging
    logging.getLogger(__name__).info(
        f"character_gen_node: outline_chars={len(state.get('outline_characters', []))}, created={len(characters)}"
    )

    new_state: NovelState = {
        **state,
        "characters": characters,
        "stage": STAGE_CHARACTERS,
    }

    return new_state
```

- [ ] **Step 2: 运行角色相关测试**

```bash
docker exec novelagent-backend-1 pytest tests/ -v -k "character" 2>&1 | tail -20
```

- [ ] **Step 3: Commit**

```bash
git add backend/app/agents/nodes/character_generation.py
git commit -m "fix(agents): convert create_characters_from_outline_node to async

Use run_in_executor for sync DB operations to avoid blocking the
async event loop. Consistent with all other LangGraph nodes.
Fixes B6."
```

---

### Task 8: 修复 P2 - 消除节点内 SessionLocal，统一 DB 会话管理

**Files:**
- Modify: `backend/app/agents/nodes/outline_generation.py`
- Modify: `backend/app/agents/nodes/character_generation.py`
- Modify: `backend/app/agents/nodes/relation_generation.py`
- Modify: `backend/app/agents/nodes/review.py`
- Modify: `backend/app/agents/nodes/rewrite.py`
- Modify: `backend/app/api/workflow.py`

- [ ] **Step 1: 修改 prepare_outline_prompt 接受 db 参数**

在 `outline_generation.py` 中修改 `prepare_outline_prompt` 函数签名：

```python
# 修改前
def prepare_outline_prompt(state: NovelState) -> tuple[str, int]:
    db = SessionLocal()
    # ...
    db.close()
    return prompt, chapter_count

# 修改后
def prepare_outline_prompt(
    state: NovelState, db: Session | None = None
) -> tuple[str, int]:
    should_close = False
    if db is None:
        db = SessionLocal()
        should_close = True
    try:
        # ... 原有逻辑，使用传入的 db
        prompt = get_system_prompt(db, "outline_generation").format(...)
        return prompt, chapter_count
    finally:
        if should_close:
            db.close()
```

- [ ] **Step 2: 修改 LangGraph 节点传递 db 会话**

在 `outline_generation_node` 中，通过 config 获取 db 会话（如果可用）：

由于 LangGraph 节点签名固定为 `(state) -> state`，需要通过另一种方式传递 db 会话。这里采用 fallback 方案：如果 `prepare_outline_prompt` 没收到 db 则自己创建、自己关闭。**关键改进是：接受可选 db 参数，使调用方可以传入已有会话。**

- [ ] **Step 3: 修改 character_generation.py 接受可选 db 参数**

```python
def extract_characters_from_outline(
    state: NovelState, db: Session | None = None
) -> list[dict]:
    should_close = False
    if db is None:
        db = SessionLocal()
        should_close = True
    try:
        # ... 原有逻辑
    finally:
        if should_close:
            db.close()
```

- [ ] **Step 4: 修改 relation_generation.py 接受可选 db 参数**

将 `write_relations_to_db` 和 `generate_relations_node` 中的 `SessionLocal()` 改为可选参数模式。

- [ ] **Step 5: 修改 review_chapter_node 接受可选 db 参数**

```python
async def review_chapter_node(
    state: NovelState,
    chapter_content: str,
    chapter_outline: dict,
    llm: LLMService,
    strictness: str = "standard",
    db: Session | None = None,
) -> dict[str, Any]:
```

- [ ] **Step 6: 修改 rewrite_chapter_node 接受可选 db 参数**

同样改为 `db: Session | None = None` 模式。

- [ ] **Step 7: 在 workflow.py 中传递 db 到持久化函数**

workflow.py 中的 `stream_generator()` 已经通过 `get_db` 依赖注入了 db，直接传递给持久化函数（Task 2 已处理）。

- [ ] **Step 8: 运行全量测试**

```bash
docker exec novelagent-backend-1 pytest -v 2>&1 | tail -30
```

- [ ] **Step 9: Commit**

```bash
git add backend/app/agents/nodes/outline_generation.py \
        backend/app/agents/nodes/character_generation.py \
        backend/app/agents/nodes/relation_generation.py \
        backend/app/agents/nodes/review.py \
        backend/app/agents/nodes/rewrite.py
git commit -m "refactor(agents): accept optional db session in node utility functions

Add optional db parameter to functions that internally create
SessionLocal. Callers can pass existing sessions to avoid
connection overhead. Part of P2 fix."
```

---

### Task 9: 修复 P4 - Checkpointer 外部会话不关闭

**Files:**
- Modify: `backend/app/agents/checkpointer.py:50-66`

- [ ] **Step 1: 修复 _close_db 逻辑**

```python
# 修改前
def _close_db(self):
    """关闭内部数据库会话，不影响外部会话"""
    if self._internal_db:
        self._internal_db.close()
        self._internal_db = None

# 修改后（逻辑不变，但需要确认 finally 块不会调用 _close_db 当使用外部会话时）
```

实际上当前代码已经正确地只在 `_internal_db` 存在时才关闭。问题在于 `get_tuple` 和 `put` 的 `finally` 块始终调用 `_close_db()`，但 `_close_db()` 内部已安全。

**真正的优化：** 在 `run_workflow` 中已经传入了 `_external_db`，确认 `create_novel_graph_with_checkpointer` 正确传递即可。当前代码已经正确，无需修改。

- [ ] **Step 1 (替代): 添加 checkpointer 会话生命周期日志**

在 `checkpointer.py` 中添加 debug 日志以监控会话创建/关闭：

```python
def _get_db(self) -> Session:
    if self._external_db is not None:
        return self._external_db
    if self._internal_db is None:
        self._internal_db = SessionLocal()
        logger.debug(
            f"Checkpointer: created internal DB session for "
            f"project {self.project_id}"
        )
    return self._internal_db
```

- [ ] **Step 2: Commit**

```bash
git add backend/app/agents/checkpointer.py
git commit -m "refactor(checkpointer): verify and document external session reuse

Current code correctly avoids closing external sessions. Add debug
logging for session lifecycle visibility. Part of P4."
```

---

### Task 10: 修复 P7 - 添加 WorkflowCheckpoint 复合索引

**Files:**
- Create: `backend/alembic/versions/xxxx_add_checkpoint_index.py`

- [ ] **Step 1: 创建 Alembic 迁移**

```bash
docker exec novelagent-backend-1 alembic revision -m "add checkpoint project thread index"
```

- [ ] **Step 2: 编写迁移代码**

```python
"""add checkpoint project thread index

Revision ID: xxxx
Revises: <parent>
Create Date: 2026-05-05

"""
from alembic import op

# revision identifiers
revision = 'xxxx'
down_revision = '<parent>'
branch_labels = None
depends_on = None


def upgrade():
    op.create_index(
        'idx_checkpoint_project_thread_updated',
        'workflow_checkpoints',
        ['project_id', 'thread_id', 'updated_at'],
        postgresql_using='btree',
    )


def downgrade():
    op.drop_index('idx_checkpoint_project_thread_updated')
```

- [ ] **Step 3: 应用迁移**

```bash
docker exec novelagent-backend-1 alembic upgrade head
```

- [ ] **Step 4: 验证索引已创建**

```bash
docker exec novelagent-db-1 psql -U novelagent -d novelagent -c \
  "\di idx_checkpoint_project_thread_updated"
```

- [ ] **Step 5: Commit**

```bash
git add backend/alembic/versions/xxxx_add_checkpoint_index.py
git commit -m "perf(db): add composite index on workflow_checkpoints

Add idx_checkpoint_project_thread_updated for frequent
(project_id, thread_id) queries. Fixes P7."
```

---

### Task 11: 添加 prompt_loader 内存缓存

**Files:**
- Modify: `backend/app/services/prompt_loader.py`

- [ ] **Step 1: 添加 Prompt 内存缓存**

```python
"""Prompt loader service with in-memory cache"""

import time
from sqlalchemy.orm import Session
from app.models.system_config import SystemConfig
from app.agents.prompts import DEFAULT_PROMPTS

# prompt 最小有效长度
MIN_PROMPT_LENGTH = 100

# 内存缓存：{key: (value, timestamp)}
_prompt_cache: dict[str, tuple[str, float]] = {}
CACHE_TTL = 300  # 缓存 5 分钟


def get_system_prompt(db: Session, agent_type: str) -> str:
    """Get system prompt for agent type from database or default

    优先使用内存缓存，其次数据库，最后代码默认值。
    """
    key = f"prompt_{agent_type}"

    # 检查缓存
    now = time.time()
    cached = _prompt_cache.get(key)
    if cached and (now - cached[1]) < CACHE_TTL:
        return cached[0]

    # 从数据库加载
    config = db.query(SystemConfig).filter(SystemConfig.key == key).first()

    if config and config.value and len(config.value.strip()) >= MIN_PROMPT_LENGTH:
        _prompt_cache[key] = (config.value, now)
        return config.value

    # 使用代码默认值
    default = DEFAULT_PROMPTS.get(agent_type, "")
    _prompt_cache[key] = (default, now)
    return default


def invalidate_prompt_cache(agent_type: str | None = None):
    """清除 Prompt 缓存

    Args:
        agent_type: 特定 agent type 的缓存，None 则清除所有
    """
    if agent_type:
        key = f"prompt_{agent_type}"
        _prompt_cache.pop(key, None)
    else:
        _prompt_cache.clear()
```

- [ ] **Step 2: 在系统提示词更新 API 中清除缓存**

在 `backend/app/api/system_prompts.py` 的 `update_system_prompt` 和 `reset_system_prompt` 中添加：

```python
from app.services.prompt_loader import invalidate_prompt_cache

# 在 db.commit() 之后添加:
invalidate_prompt_cache(agent_type)
```

- [ ] **Step 3: 运行测试**

```bash
docker exec novelagent-backend-1 pytest tests/ -v -k "prompt" 2>&1 | tail -20
```

- [ ] **Step 4: Commit**

```bash
git add backend/app/services/prompt_loader.py backend/app/api/system_prompts.py
git commit -m "perf(prompts): add in-memory TTL cache for system prompts

Cache prompts for 5 minutes to avoid repeated database queries during
workflow execution. Invalidate cache when prompts are updated.
Fixes P6."
```

---

### Task 12: 批量查询优化 list_projects

**Files:**
- Modify: `backend/app/api/projects.py:70-90`

- [ ] **Step 1: 优化 list_projects 使用批量查询**

```python
@router.get("/", response_model=ProjectListResponse)
async def list_projects(
    limit: Optional[int] = None,
    offset: int = 0,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user),
):
    """列出当前用户的所有项目，使用批量查询避免 N+1"""
    from sqlalchemy import func as sa_func
    from sqlalchemy.orm import joinedload

    projects = (
        db.query(Project)
        .filter(Project.user_id == current_user.id)
    )
    total = projects.count()

    if limit is not None:
        projects = projects.offset(offset).limit(limit).all()
    else:
        projects = projects.all()

    if not projects:
        return ProjectListResponse(projects=[], total=0)

    project_ids = [p.id for p in projects]

    # 批量查询章节进度
    chapter_stats = (
        db.query(
            ChapterOutline.project_id,
            sa_func.count(ChapterOutline.id).label("total_chapters"),
            sa_func.count(Chapter.id).label("completed_chapters"),
        )
        .outerjoin(Chapter, Chapter.chapter_outline_id == ChapterOutline.id)
        .filter(ChapterOutline.project_id.in_(project_ids))
        .filter(Chapter.review_passed == True)
        .group_by(ChapterOutline.project_id)
        .all()
    )
    stats_map = {s.project_id: (s.total_chapters, s.completed_chapters or 0) for s in chapter_stats}

    # 批量查询工作流状态
    workflow_states = (
        db.query(WorkflowState)
        .filter(WorkflowState.project_id.in_(project_ids))
        .all()
    )
    workflow_map = {ws.project_id: ws for ws in workflow_states}

    # 构建响应
    project_details = []
    for p in projects:
        total_ch, completed_ch = stats_map.get(p.id, (0, 0))
        ws = workflow_map.get(p.id)

        progress = round((completed_ch / total_ch * 100), 1) if total_ch > 0 else 0
        project_details.append(
            ProjectDetailResponse(
                id=p.id,
                user_id=p.user_id,
                name=p.name,
                target_words=p.target_words,
                total_words=p.total_words,
                created_at=p.created_at,
                updated_at=p.updated_at,
                workflow_state=WorkflowStateResponse.model_validate(
                    ws
                ) if ws else None,
                chapter_count=total_ch,
                completed_chapters=completed_ch,
                progress_percentage=progress,
            )
        )

    return ProjectListResponse(projects=project_details, total=total)
```

- [ ] **Step 2: 运行项目列表测试**

```bash
docker exec novelagent-backend-1 pytest tests/ -v -k "project" 2>&1 | tail -20
```

- [ ] **Step 3: Commit**

```bash
git add backend/app/api/projects.py
git commit -m "perf(api): use batch queries in list_projects to avoid N+1

Replace per-project get_project_detail calls with single bulk queries
for chapter progress and workflow states. Fixes P3."
```

---

### Task 13: LLMService 添加重试逻辑

**Files:**
- Modify: `backend/app/services/llm.py`

- [ ] **Step 1: 添加指数退避重试装饰器**

```python
"""LLM service with retry logic"""

import asyncio
import logging
from typing import AsyncIterator
from openai import AsyncOpenAI, OpenAIError

from app.config import settings

logger = logging.getLogger(__name__)

# 可重试的 HTTP 状态码
RETRYABLE_STATUS_CODES = {429, 500, 502, 503, 504}
MAX_RETRIES = 3
BASE_DELAY = 1.0  # 秒


class LLMService:
    """LLM service for generating content with retry support"""

    # ... MODEL_CONFIGS 和 __init__ 保持不变 ...

    async def chat(
        self,
        messages: list[dict],
        temperature: float = 0.7,
        max_tokens: int = 4096,
    ) -> str:
        """Send a chat request and get response with retry"""
        last_error = None
        for attempt in range(MAX_RETRIES + 1):
            try:
                response = await self.client.chat.completions.create(
                    model=self.model,
                    messages=messages,
                    temperature=temperature,
                    max_tokens=max_tokens,
                )
                return response.choices[0].message.content
            except OpenAIError as e:
                last_error = e
                if not self._should_retry(e, attempt):
                    break
                delay = BASE_DELAY * (2**attempt)
                logger.warning(
                    f"LLM chat attempt {attempt + 1} failed: {e}. "
                    f"Retrying in {delay:.1f}s..."
                )
                await asyncio.sleep(delay)
        raise last_error  # type: ignore[misc]

    async def chat_stream(
        self,
        messages: list[dict],
        temperature: float = 0.7,
        max_tokens: int = 4096,
    ) -> AsyncIterator[str]:
        """Send a chat request and stream response with retry"""
        last_error = None
        for attempt in range(MAX_RETRIES + 1):
            try:
                stream = await self.client.chat.completions.create(
                    model=self.model,
                    messages=messages,
                    temperature=temperature,
                    max_tokens=max_tokens,
                    stream=True,
                )
                async for chunk in stream:
                    if chunk.choices[0].delta.content:
                        yield chunk.choices[0].delta.content
                return  # 成功，退出
            except OpenAIError as e:
                last_error = e
                if not self._should_retry(e, attempt):
                    break
                delay = BASE_DELAY * (2**attempt)
                logger.warning(
                    f"LLM chat_stream attempt {attempt + 1} failed: {e}. "
                    f"Retrying in {delay:.1f}s..."
                )
                await asyncio.sleep(delay)
        raise last_error  # type: ignore[misc]

    def _should_retry(self, error: OpenAIError, attempt: int) -> bool:
        """判断是否应该重试"""
        if attempt >= MAX_RETRIES:
            return False
        status_code = getattr(error, "status_code", None)
        if status_code in RETRYABLE_STATUS_CODES:
            return True
        # 网络错误也重试
        error_str = str(error).lower()
        if any(
            kw in error_str
            for kw in ["timeout", "connection", "network", "reset"]
        ):
            return True
        return False

    async def chat_with_system(
        self, system_prompt: str, messages: list[dict], temperature: float = 0.7
    ) -> str:
        """Chat with system prompt"""
        full_messages = [{"role": "system", "content": system_prompt}] + messages
        return await self.chat(full_messages, temperature)
```

- [ ] **Step 2: 运行全量测试**

```bash
docker exec novelagent-backend-1 pytest -v 2>&1 | tail -30
```

- [ ] **Step 3: Commit**

```bash
git add backend/app/services/llm.py
git commit -m "feat(llm): add exponential backoff retry for LLM API calls

Retry on 429/5xx status codes and network errors up to 3 times
with exponential backoff. Fixes Q8."
```

---

### Task 14: 运行全量测试并验证

- [ ] **Step 1: 运行全量后端测试**

```bash
docker exec novelagent-backend-1 pytest -v 2>&1
```

确认所有测试通过。

- [ ] **Step 2: 运行前端测试**

```bash
cd frontend && npm run test 2>&1
```

确认所有测试通过。

- [ ] **Step 3: 构建并验证容器**

```bash
docker compose build backend && docker compose up -d backend
```

确认容器正常启动。

- [ ] **Step 4: Commit 剩余文件**

```bash
git add -A
git commit -m "chore: finalize v0.8.1 optimization changes"
```
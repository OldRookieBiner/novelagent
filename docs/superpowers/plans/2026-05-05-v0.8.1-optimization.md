# v0.8.1 优化实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复项目删除 500 错误 + 灵感页面添加模型选择器

**Architecture:** 两个独立子系统：后端级联删除修复（1 个模型文件）和前端模型选择器（InspirationPanel + OutlineProgressDialog + workflowApi）。前端使用 shadcn/ui Select 组件，按提供商分组展示所有已启用模型，选中后传递 model_config_id 给工作流 SSE API。后端已有完整 llm_config_id 支持链路，无需修改。

**Tech Stack:** Python 3 / FastAPI / SQLAlchemy / React 18 / TypeScript / shadcn-ui / Zustand

---

### Task 1: 修复项目删除 — 添加 WorkflowCheckpoint 级联关系

**Files:**
- Modify: `backend/app/models/project.py`
- Modify: `backend/app/models/checkpoint.py`

#### 1.1 检查现有关系定义

```bash
docker exec novelagent-backend-1 python -c "
from app.models.project import Project
from app.models.checkpoint import WorkflowCheckpoint
# 验证 Project 没有 checkpoints 关系
print('project has checkpoints:', hasattr(Project, 'checkpoints'))
# 验证 WorkflowCheckpoint 使用 backref
import inspect
src = inspect.getsource(WorkflowCheckpoint)
print('backref in checkpoint:', 'backref' in src)
"
```

Expected: `project has checkpoints: False` (通过 backref 隐式存在但无 cascade)，`backref in checkpoint: True`

#### 1.2 修改 WorkflowCheckpoint — backref → back_populates

```python
# backend/app/models/checkpoint.py — 第 29 行
# 改为显式 back_populates
project = relationship("Project", back_populates="checkpoints")
```

#### 1.3 修改 Project — 添加 checkpoints 关系

```python
# backend/app/models/project.py — 在 relations 关系后面添加
checkpoints = relationship(
    "WorkflowCheckpoint", back_populates="project", cascade="all, delete-orphan"
)
```

#### 1.4 验证关系配置正确

```bash
docker exec novelagent-backend-1 python -c "
from app.models.project import Project
from app.models.checkpoint import WorkflowCheckpoint
# 验证双向关系配置正确
assert Project.checkpoints.property.back_populates == 'project'
assert WorkflowCheckpoint.project.property.back_populates == 'checkpoints'
# 验证 cascade 配置
assert 'all, delete-orphan' in str(Project.checkpoints.property.cascade)
print('All relationship checks passed')
"
```

Expected: `All relationship checks passed`

#### 1.5 重启后端并验证删除接口

```bash
docker compose restart backend
sleep 5
# 通过 API 测试删除一个存在的项目
docker exec novelagent-backend-1 python -c "
from app.database import SessionLocal
from app.models.project import Project
# 验证数据库连接正常
db = SessionLocal()
count = db.query(Project).count()
print(f'Projects in DB: {count}')
db.close()
"
```

Expected: 返回项目数量，无错误

---

### Task 2: 添加模型选择器数据流 — modelConfigsApi.list() 集成

**Files:**
- Modify: `frontend/src/components/workbench/planning/InspirationPanel.tsx`

#### 2.1 添加模型列表状态和获取逻辑

添加 imports（在现有 imports 后追加）：

```typescript
// 追加到现有 imports
import { modelConfigsApi } from '@/lib/api.ts'
import type { ModelConfig } from '@/types'
import { Cpu } from 'lucide-react'
import {
  Select,
  SelectContent,
  SelectGroup,
  SelectItem,
  SelectLabel,
  SelectTrigger,
  SelectValue,
} from '@/components/ui/select'
```

注意：`modelConfigsApi` 已从 `@/lib/api` 导出，`ModelConfig` 类型已在 `@/types` 中定义，`Select` 系列组件位于 `@/components/ui/select`。`Cpu` 来自 `lucide-react`。

已有 `lucide-react` import 行，追加 `Cpu` 到现有 import：

```typescript
// 修改现有的 lucide-react import 行（第 4 行）
import { Lightbulb, RotateCcw, ArrowRight, Check, ChevronDown, ChevronUp, Copy, ChevronLeft, ChevronRight, Cpu } from 'lucide-react'
```

已有 `modelConfigsApi` 在 `@/lib/api` 中导出（api.ts 第 488 行），TypeScript 类型 `ModelConfig` 在 `@/types` 中定义（types/index.ts 第 286 行），Select 组件在 `@/components/ui/select` 中（select.tsx）。

#### 2.2 定义模型选项数据结构

在 `InspirationPanelProps` interface 后面添加类型定义：

```typescript
/** 扁平化后的模型选项 */
interface ModelOption
{
  modelConfigId: number  // model_configs 表 ID
  modelName: string      // 具体模型名
  providerName: string   // 提供商显示名
  provider: string       // 提供商标识
  isDefault: boolean     // 是否为默认配置
}
```

#### 2.3 在组件内添加模型选择器状态

在现有状态声明后（`const [showProgressDialog, setShowProgressDialog] = useState(false)` 之后，第 98 行附近）添加：

```typescript
// 模型选择器状态
const [modelOptions, setModelOptions] = useState<ModelOption[]>([])
const [selectedModelKey, setSelectedModelKey] = useState<string>('')
const [loadingModels, setLoadingModels] = useState(false)
```

#### 2.4 添加模型列表获取 useEffect

在组件中已有的 `useEffect` 块后面（`loadInspirationDraft()` useEffect 后面，第 171 行后）添加：

```typescript
// 加载可用模型列表
useEffect(() =>
{
  const loadModels = async () =>
  {
    setLoadingModels(true)
    try
    {
      const response = await modelConfigsApi.list()
      const options: ModelOption[] = []
      // 提供商的显示名称映射
      const providerNames: Record<string, string> = {
        deepseek: 'DeepSeek (深度求索)',
        openai: 'OpenAI',
        anthropic: 'Anthropic',
        baidu: '百度文心',
        volcengine: '火山引擎',
        unicom: '联通云',
        custom: '自定义',
      }
      for (const config of response.models)
      {
        // 只显示已启用的配置
        if (!config.is_enabled) continue
        const providerDisplayName = providerNames[config.provider] || config.provider
        if (config.provider_type === 'coding_plan' && config.models && config.models.length > 0)
        {
          // coding_plan 类型：展开 models 数组，每个 model 一个选项
          for (const model of config.models)
          {
            if (!model.is_enabled) continue
            options.push({
              modelConfigId: config.id,
              modelName: model.name,
              providerName: providerDisplayName,
              provider: config.provider,
              isDefault: config.is_default,
            })
          }
        }
        else if (config.model_name)
        {
          // single 类型：一个配置一个选项
          options.push({
            modelConfigId: config.id,
            modelName: config.model_name,
            providerName: providerDisplayName,
            provider: config.provider,
            isDefault: config.is_default,
          })
        }
      }
      setModelOptions(options)
      // 设置默认选中：优先 is_default === true 的配置
      const defaultOption = options.find(o => o.isDefault) || options[0]
      if (defaultOption)
      {
        // key 格式: "model_config_id:model_name"
        setSelectedModelKey(`${defaultOption.modelConfigId}:${defaultOption.modelName}`)
      }
    }
    catch (err)
    {
      console.error('Failed to load model options:', err)
    }
    finally
    {
      setLoadingModels(false)
    }
  }
  loadModels()
}, [])
```

#### 2.5 修改底部操作栏：添加模型选择器

替换底部操作栏 JSX（现有第 789-803 行）：

```tsx
{/* 底部：模型选择 + 确认按钮 */}
<div className="border-t bg-white px-6 py-4">
  <div className="flex items-center gap-3 max-w-2xl mx-auto">
    {/* AI 模型选择器 */}
    <div className="flex-1 min-w-0">
      <label className="text-xs text-muted-foreground mb-1.5 flex items-center gap-1" htmlFor="model-select-trigger">
        <Cpu className="h-3 w-3" />
        AI 模型
      </label>
      <Select value={selectedModelKey} onValueChange={setSelectedModelKey} disabled={loadingModels || modelOptions.length === 0}>
        <SelectTrigger id="model-select-trigger" className="w-full h-9 text-sm">
          <SelectValue placeholder={loadingModels ? '加载中...' : '请选择模型'} />
        </SelectTrigger>
        <SelectContent>
          {/* 按提供商分组 */}
          {(() =>
          {
            // 按 provider 分组
            const grouped = new Map<string, ModelOption[]>()
            for (const opt of modelOptions)
            {
              if (!grouped.has(opt.provider))
              {
                grouped.set(opt.provider, [])
              }
              grouped.get(opt.provider)!.push(opt)
            }
            return Array.from(grouped.entries()).map(([provider, options]) => (
              <SelectGroup key={provider}>
                <SelectLabel>{options[0].providerName}</SelectLabel>
                {options.map(opt => (
                  <SelectItem
                    key={`${opt.modelConfigId}:${opt.modelName}`}
                    value={`${opt.modelConfigId}:${opt.modelName}`}
                  >
                    <span className="flex items-center gap-1.5">
                      {opt.modelName}
                      {opt.isDefault && (
                        <span className="text-[10px] bg-green-100 text-green-700 px-1.5 py-0.5 rounded">默认</span>
                      )}
                    </span>
                  </SelectItem>
                ))}
              </SelectGroup>
            ))
          })()}
        </SelectContent>
      </Select>
    </div>

    {/* 确认按钮 */}
    <Button onClick={handleConfirm} disabled={confirming} className="px-6 mt-[22px]">
      {confirming ? (
        <>保存中...</>
      ) : (
        <>
          <Check className="h-4 w-4 mr-2" />
          开始规划
          <ArrowRight className="h-4 w-4 ml-2" />
        </>
      )}
    </Button>
  </div>
  <p className="text-xs text-muted-foreground text-center mt-1.5">确认后自动开始规划</p>
</div>
```

#### 2.6 修改 handleConfirm — 传递 selectedModelKey

在 `handleConfirm` 函数中，在 `collectedInfoData` 构建完成后（第 316 行 `setShowProgressDialog(true)` 之前），添加解析模型选择：

```typescript
// 解析选中的模型信息
if (selectedModelKey)
{
  const [configIdStr, ...modelNameParts] = selectedModelKey.split(':')
  const configId = parseInt(configIdStr)
  // modelName 可能包含冒号（如 claude-sonnet-4-20250514），重新拼接
  const modelName = modelNameParts.join(':')
  if (!isNaN(configId) && modelName)
  {
    collectedInfoData.model_config_id = configId
    collectedInfoData.model_name = modelName
  }
}
```

---

### Task 3: OutlineProgressDialog — 接收并传递 modelConfigId

**Files:**
- Modify: `frontend/src/components/workbench/planning/OutlineProgressDialog.tsx`
- Modify: `frontend/src/components/workbench/planning/InspirationPanel.tsx`

#### 3.1 添加 modelConfigId prop

在 `OutlineProgressDialogProps` interface 中添加：

```typescript
/** 可选的模型配置 ID */
modelConfigId?: number
```

#### 3.2 解构 props

```typescript
export function OutlineProgressDialog({
  open,
  onClose,
  projectId,
  modelConfigId,
  onComplete,
  onViewOutline,
}: OutlineProgressDialogProps)
```

#### 3.3 修改 workflowApi.runWorkflow 调用

在 `handleGenerate` 中（第 107 行），调用 `runWorkflow` 时传入 `llmConfigId`：

```typescript
await workflowApi.runWorkflow(
  projectId,
  {
    // ... callbacks 保持不变
  },
  { signal: controller.signal, llmConfigId: modelConfigId }
)
```

#### 3.4 InspirationPanel — 传递 modelConfigId 给 OutlineProgressDialog

修改 InspirationPanel 底部 JSX 中的 `OutlineProgressDialog` 调用：

```tsx
<OutlineProgressDialog
  open={showProgressDialog}
  onClose={() => setShowProgressDialog(false)}
  projectId={projectId}
  modelConfigId={selectedModelKey ? parseInt(selectedModelKey.split(':')[0]) : undefined}
  onComplete={() => {}}
  onViewOutline={() =>
  {
    setShowProgressDialog(false)
    setActiveMenuItem('outline')
  }}
/>
```

---

### Task 4: workflowApi — runWorkflow 支持 body 参数

**Files:**
- Modify: `frontend/src/lib/workflowApi.ts`

#### 4.1 添加 llmConfigId 支持

修改 `StreamOptions` 的定义（当前在 `api.ts` 中）：

`StreamOptions` 定义在 `api.ts`（第 235 行）：

```typescript
export interface StreamOptions {
  signal?: AbortSignal;  // 用于取消请求
}
```

在第 235 行修改：

```typescript
export interface StreamOptions {
  signal?: AbortSignal;  // 用于取消请求
  llmConfigId?: number;  // 可选的模型配置 ID
}
```

#### 4.2 修改 workflowApi.runWorkflow

将 `llmConfigId` 传递给 SSE 请求 body：

```typescript
// 使用统一的 SSE 流处理器
const requestBody: Record<string, unknown> = {}
if (options?.llmConfigId)
{
  requestBody.llm_config_id = options.llmConfigId
}
await createSSEStream(
  {
    url: `/api/projects/${projectId}/workflow/run`,
    method: 'POST',
    signal: options?.signal,
    body: Object.keys(requestBody).length > 0 ? requestBody : undefined,
  },
  handleEvent,
  (error) => callbacks.onError?.(error)
)
```

---

### Task 5: 验证 — 运行测试并确认功能

#### 5.1 重启服务

```bash
docker compose restart backend && sleep 3
```

#### 5.2 后端验证

```bash
# 验证项目删除不再 500
docker exec novelagent-backend-1 python -c "
from app.database import SessionLocal
from app.models.project import Project
from app.models.checkpoint import WorkflowCheckpoint

db = SessionLocal()
try:
    # 创建一个测试项目并添加 checkpoint
    project = Project(name='test-cascade-delete', user_id=1, novel_length=100000)
    db.add(project)
    db.flush()
    checkpoint = WorkflowCheckpoint(
        project_id=project.id,
        thread_id='test-thread',
        checkpoint={'test': True}
    )
    db.add(checkpoint)
    db.commit()
    
    # 尝试删除项目
    db.delete(project)
    db.commit()
    print('SUCCESS: Project cascade-deleted with checkpoint')
except Exception as e:
    db.rollback()
    print(f'FAIL: {e}')
finally:
    db.close()
"
```

Expected: `SUCCESS: Project cascade-deleted with checkpoint`

#### 5.3 前端构建验证

```bash
cd frontend && npx tsc --noEmit 2>&1 | head -20
```

Expected: 无 TypeScript 错误

#### 5.4 运行现有测试

```bash
docker exec novelagent-backend-1 pytest tests/ -v --tb=short
cd frontend && npm run test:run
```

Expected: 所有现有测试通过

---

### Task 6: 提交

```bash
git add backend/app/models/project.py backend/app/models/checkpoint.py
git add frontend/src/components/workbench/planning/InspirationPanel.tsx
git add frontend/src/components/workbench/planning/OutlineProgressDialog.tsx
git add frontend/src/lib/workflowApi.ts
git add frontend/src/lib/api.ts
git commit -m "fix(backend): add cascade delete for workflow checkpoints
feat(frontend): add model selector to inspiration panel"
```
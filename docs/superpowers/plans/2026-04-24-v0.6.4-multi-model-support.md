# v0.6.4 多模型配置与体验优化实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 重构模型配置系统，支持多 AI 模型配置与 Coding Plan 类型 API，优化用户体验。

**Architecture:** 后端新增 `provider_type` 和 `models` 字段支持多模型配置，前端采用列表分组 UI 展示，灵感采集页新增模型选择下拉框。

**Tech Stack:** FastAPI, SQLAlchemy, Alembic, React, shadcn/ui, Sonner Toast

---

## 文件结构

### 后端文件

| 文件 | 职责 |
|------|------|
| `backend/app/models/model_config.py` | 数据模型，新增字段 |
| `backend/app/schemas/model_config.py` | Pydantic schemas |
| `backend/app/api/model_configs.py` | API 端点，新增 fetch-models |
| `backend/app/services/model_providers.py` | 预设提供商配置（新建） |
| `backend/app/agents/nodes/chapter_generation.py` | 章节内容后处理 |
| `backend/app/api/workflow.py` | 支持 llm_config_id 参数 |
| `backend/alembic/versions/xxxx_add_provider_type_models.py` | 数据库迁移（新建） |

### 前端文件

| 文件 | 职责 |
|------|------|
| `frontend/src/pages/Writing.tsx` | 按钮调整 |
| `frontend/src/pages/Home.tsx` | alert 替换为 toast |
| `frontend/src/pages/ProjectDetail.tsx` | alert 替换为 toast |
| `frontend/src/components/project/OutlineWorkflow.tsx` | alert 替换为 toast |
| `frontend/src/components/ui/sonner.tsx` | Toast 组件（如不存在则新建） |
| `frontend/src/App.tsx` | 添加 Toaster |
| `frontend/src/pages/Settings.tsx` | 模型配置页面重构 |
| `frontend/src/components/settings/ModelConfigItem.tsx` | 模型配置项组件（新建） |
| `frontend/src/components/settings/AddModelDialog.tsx` | 添加模型对话框（新建） |
| `frontend/src/components/project/InspirationForm.tsx` | 新增模型选择 |
| `frontend/src/lib/api.ts` | 新增 API 方法 |
| `frontend/src/types/index.ts` | 更新类型定义 |

---

## Phase 1: 快速优化

### Task 1: 章节正文结尾去除数字

**Files:**
- Modify: `backend/app/agents/nodes/chapter_generation.py:420-430`
- Test: `backend/tests/test_chapter_content_clean.py`（新建）

- [ ] **Step 1: 编写测试用例**

创建测试文件：

```python
# backend/tests/test_chapter_content_clean.py
import pytest
from app.agents.nodes.chapter_generation import clean_chapter_content


class TestCleanChapterContent:
    """测试章节内容清理函数"""

    def test_removes_trailing_number_with_newlines(self):
        """测试移除结尾数字（带换行）"""
        content = "正文内容\n\n3247"
        result = clean_chapter_content(content)
        assert result == "正文内容"

    def test_removes_trailing_number_single_newline(self):
        """测试移除结尾数字（单换行）"""
        content = "正文内容\n3247"
        result = clean_chapter_content(content)
        assert result == "正文内容"

    def test_preserves_number_in_paragraph(self):
        """测试保留段落中的数字"""
        content = "正文内容3247"
        result = clean_chapter_content(content)
        assert result == "正文内容3247"

    def test_no_change_without_trailing_number(self):
        """测试无结尾数字时不变"""
        content = "正文内容"
        result = clean_chapter_content(content)
        assert result == "正文内容"

    def test_removes_multiple_trailing_numbers(self):
        """测试移除多个结尾数字行"""
        content = "正文内容\n3247\n\n5678"
        result = clean_chapter_content(content)
        assert result == "正文内容"

    def test_handles_empty_content(self):
        """测试空内容"""
        content = ""
        result = clean_chapter_content(content)
        assert result == ""
```

- [ ] **Step 2: 运行测试确认失败**

```bash
docker exec novelagent-backend-1 pytest tests/test_chapter_content_clean.py -v
```

Expected: FAIL（函数不存在）

- [ ] **Step 3: 实现清理函数**

在 `backend/app/agents/nodes/chapter_generation.py` 顶部添加导入和函数：

```python
import re
from typing import Dict, Any, AsyncIterator


def clean_chapter_content(content: str) -> str:
    """清理章节内容，移除 LLM 可能添加的结尾数字

    Args:
        content: 原始章节内容

    Returns:
        清理后的章节内容

    Examples:
        >>> clean_chapter_content("正文内容\\n\\n3247")
        '正文内容'
        >>> clean_chapter_content("正文内容3247")
        '正文内容3247'
    """
    if not content:
        return content

    # 移除结尾的纯数字行（可能是 LLM 自动添加的字数）
    # 只匹配独立的数字行，不匹配段落中的数字
    return re.sub(r'\n*\d+\s*$', '', content.strip())
```

- [ ] **Step 4: 在生成节点中调用清理函数**

修改 `generate_chapter_content_node` 函数，在 `word_count` 计算前添加清理：

```python
async def generate_chapter_content_node(state: NovelState) -> NovelState:
    # ... 现有代码直到 content = await llm.chat(...) ...

    # 调用 LLM 生成内容
    content = await llm.chat([{"role": "user", "content": prompt}])

    # 后处理：移除结尾的纯数字（可能是 LLM 自动添加的字数）
    content = clean_chapter_content(content)

    # 计算字数
    word_count = len(content)

    # ... 后续代码不变 ...
```

- [ ] **Step 5: 运行测试确认通过**

```bash
docker exec novelagent-backend-1 pytest tests/test_chapter_content_clean.py -v
```

Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add backend/app/agents/nodes/chapter_generation.py backend/tests/test_chapter_content_clean.py
git commit -m "feat(backend): clean trailing numbers from chapter content

- Add clean_chapter_content function with regex
- Apply cleaning in generate_chapter_content_node
- Add unit tests for edge cases

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

### Task 2: Writing 页面按钮调整

**Files:**
- Modify: `frontend/src/pages/Writing.tsx:351-387`

- [ ] **Step 1: 修改按钮逻辑**

找到操作按钮部分（约 351-387 行），替换为：

```tsx
{/* 操作按钮 */}
<div className="px-4 py-3 border-t bg-muted/30 flex gap-2">
  {mode === 'edit' ? (
    <>
      <Button onClick={handleSave} disabled={isSaving}>
        {isSaving ? '保存中...' : '保存'}
      </Button>
      <Button variant="outline" onClick={() => setMode('preview')}>
        返回预览
      </Button>
    </>
  ) : isGenerating ? (
    <Button variant="destructive" onClick={handleStop}>
      停止生成
    </Button>
  ) : (
    <>
      <Button onClick={handleGenerate}>
        {wordCount > 0 ? '重新生成' : 'AI 生成'}
      </Button>
      {wordCount > 0 && (
        <>
          <Button variant="outline" onClick={() => setMode('edit')}>
            编辑
          </Button>
          <Button
            variant="outline"
            onClick={() => navigate(`/project/${id}/read/${currentChapter.chapter_number}`)}
          >
            审核
          </Button>
        </>
      )}
    </>
  )}
</div>
```

- [ ] **Step 2: 重启前端验证**

```bash
docker compose restart frontend
```

在浏览器中验证：
1. 打开写作页面，内容为空时按钮显示「AI 生成」
2. 生成内容后按钮变为「重新生成」「编辑」「审核」

- [ ] **Step 3: 提交**

```bash
git add frontend/src/pages/Writing.tsx
git commit -m "fix(frontend): adjust Writing page buttons

- Remove duplicate AI generate button
- Rename to '重新生成' when content exists
- Button order: 重新生成 -> 编辑 -> 审核

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

### Task 3: 添加 Toast 组件并替换 alert()

**Files:**
- Create: `frontend/src/components/ui/sonner.tsx`（如不存在）
- Modify: `frontend/src/App.tsx`
- Modify: `frontend/src/pages/Home.tsx`
- Modify: `frontend/src/pages/ProjectDetail.tsx`
- Modify: `frontend/src/components/project/OutlineWorkflow.tsx`

- [ ] **Step 1: 检查并安装 Sonner**

```bash
cd frontend && ls src/components/ui/sonner.tsx 2>/dev/null || echo "NOT_FOUND"
```

如果输出 `NOT_FOUND`，执行：

```bash
cd frontend && npx shadcn@latest add sonner
```

- [ ] **Step 2: 在 App.tsx 添加 Toaster**

修改 `frontend/src/App.tsx`：

```tsx
import { Toaster } from "@/components/ui/sonner"

// 在 Router 配置后、组件返回的最外层添加 Toaster
function App() {
  return (
    <QueryClientProvider client={queryClient}>
      <Router>
        {/* 现有路由 */}
      </Router>
      <Toaster />
    </QueryClientProvider>
  )
}
```

- [ ] **Step 3: 替换 Home.tsx 中的 alert**

修改 `frontend/src/pages/Home.tsx`：

添加导入：
```tsx
import { toast } from "sonner"
```

找到第 85 行附近，替换：
```tsx
// 修改前
alert(err instanceof Error ? err.message : '删除项目失败')

// 修改后
toast.error(err instanceof Error ? err.message : '删除项目失败')
```

- [ ] **Step 4: 替换 ProjectDetail.tsx 中的 alert**

修改 `frontend/src/pages/ProjectDetail.tsx`：

添加导入：
```tsx
import { toast } from "sonner"
```

找到第 194 行附近，替换：
```tsx
// 修改前
alert('确认章节大纲失败')

// 修改后
toast.error('确认章节大纲失败')
```

- [ ] **Step 5: 替换 OutlineWorkflow.tsx 中的所有 alert**

修改 `frontend/src/components/project/OutlineWorkflow.tsx`：

添加导入：
```tsx
import { toast } from "sonner"
```

替换所有 alert 调用：

```tsx
// 第 126 行附近
// 修改前
alert(`生成大纲失败: ${error}`)
// 修改后
toast.error(`生成大纲失败: ${error}`)

// 第 131 行附近
// 修改前
alert('生成大纲失败，请检查 API Key 配置')
// 修改后
toast.error('生成大纲失败，请检查 API Key 配置')

// 第 150 行附近
// 修改前
alert('保存大纲失败')
// 修改后
toast.error('保存大纲失败')

// 第 164 行附近
// 修改前
alert('确认大纲失败')
// 修改后
toast.error('确认大纲失败')

// 第 192 行附近
// 修改前
alert(`生成章节大纲失败: ${error}`)
// 修改后
toast.error(`生成章节大纲失败: ${error}`)

// 第 197 行附近
// 修改前
alert('生成章节大纲失败，请检查 API Key 配置')
// 修改后
toast.error('生成章节大纲失败，请检查 API Key 配置')
```

- [ ] **Step 6: 验证修改**

```bash
docker compose restart frontend
```

在浏览器中测试错误提示是否使用 Toast 显示。

- [ ] **Step 7: 提交**

```bash
git add frontend/src/App.tsx frontend/src/pages/Home.tsx frontend/src/pages/ProjectDetail.tsx frontend/src/components/project/OutlineWorkflow.tsx
git commit -m "refactor(frontend): replace alert() with Sonner toast

- Add Toaster to App.tsx
- Replace all alert() calls with toast.error()
- Better UX with non-blocking notifications

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

## Phase 2: 模型配置后端

### Task 4: 更新 ModelConfig 模型

**Files:**
- Modify: `backend/app/models/model_config.py`

- [ ] **Step 1: 添加新字段**

修改 `backend/app/models/model_config.py`：

```python
"""模型配置数据模型"""

from datetime import datetime
from sqlalchemy import Column, Integer, String, Text, Boolean, DateTime, ForeignKey, JSON
from sqlalchemy.orm import relationship

from app.database import Base


class ModelConfig(Base):
    """模型配置模型"""

    __tablename__ = "model_configs"

    id = Column(Integer, primary_key=True, index=True)
    user_id = Column(Integer, ForeignKey("users.id", ondelete="CASCADE"), nullable=False)
    name = Column(String(100), nullable=False)  # 显示名称
    provider = Column(String(50), nullable=False)  # 预设标识：deepseek, baidu, volcengine, unicom, custom
    provider_type = Column(String(20), default="single")  # "single" | "coding_plan"
    base_url = Column(String(500), nullable=False)  # API 基础地址
    model_name = Column(String(100), nullable=True)  # 单模型时使用，coding_plan 时可为空
    models = Column(JSON, nullable=True)  # coding_plan 类型存储模型列表
    api_key_encrypted = Column(Text, nullable=True)  # 加密的 API Key
    is_enabled = Column(Boolean, default=True)  # 是否启用
    is_default = Column(Boolean, default=False)  # 是否为默认模型
    last_health_check = Column(DateTime, nullable=True)  # 上次健康检查时间
    health_status = Column(String(20), nullable=True)  # healthy / unhealthy / unknown
    health_latency = Column(Integer, nullable=True)  # 延迟毫秒数
    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)

    # 关联用户
    user = relationship("User", back_populates="model_configs")

    def __repr__(self):
        return f"<ModelConfig id={self.id} name={self.name} user_id={self.user_id}>"
```

- [ ] **Step 2: 提交**

```bash
git add backend/app/models/model_config.py
git commit -m "feat(db): add provider_type and models fields to ModelConfig

- provider_type: 'single' | 'coding_plan'
- models: JSON field for coding_plan model list

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

### Task 5: 创建数据库迁移

**Files:**
- Create: `backend/alembic/versions/xxxx_add_provider_type_models.py`

- [ ] **Step 1: 生成迁移文件**

```bash
docker exec novelagent-backend-1 alembic revision --autogenerate -m "add_provider_type_and_models"
```

- [ ] **Step 2: 检查生成的迁移文件**

查看生成的文件，确认包含：
- 添加 `provider_type` 列
- 添加 `models` 列
- 设置默认值

- [ ] **Step 3: 修改迁移文件添加数据初始化**

编辑生成的迁移文件，在 `upgrade()` 中添加：

```python
def upgrade():
    # 添加字段
    op.add_column('model_configs', sa.Column('provider_type', sa.String(20), nullable=True))
    op.add_column('model_configs', sa.Column('models', sa.JSON(), nullable=True))

    # 设置默认值
    op.execute("UPDATE model_configs SET provider_type = 'single' WHERE provider_type IS NULL")

    # 设置非空约束
    op.alter_column('model_configs', 'provider_type', nullable=False)


def downgrade():
    op.drop_column('model_configs', 'models')
    op.drop_column('model_configs', 'provider_type')
```

- [ ] **Step 4: 执行迁移**

```bash
docker exec novelagent-backend-1 alembic upgrade head
```

- [ ] **Step 5: 验证迁移**

```bash
docker exec novelagent-backend-1 python -c "
from app.database import SessionLocal
from app.models.model_config import ModelConfig
db = SessionLocal()
configs = db.query(ModelConfig).all()
for c in configs:
    print(f'ID: {c.id}, provider_type: {c.provider_type}')
db.close()
"
```

- [ ] **Step 6: 提交**

```bash
git add backend/alembic/versions/*add_provider_type*.py
git commit -m "feat(db): add migration for provider_type and models

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

### Task 6: 创建预设提供商配置

**Files:**
- Create: `backend/app/services/model_providers.py`

- [ ] **Step 1: 创建配置文件**

```python
# backend/app/services/model_providers.py
"""预设模型提供商配置"""

from typing import TypedDict, Optional


class ProviderConfig(TypedDict):
    """提供商配置"""
    name: str  # 显示名称
    base_url: str  # API 基础地址
    provider_type: str  # "single" | "coding_plan"
    default_model: Optional[str]  # 单模型时的默认模型
    models_api: Optional[str]  # 获取模型列表的端点
    auth_type: str  # "bearer" | "access_token"


PRESET_PROVIDERS: dict[str, ProviderConfig] = {
    "deepseek": {
        "name": "DeepSeek",
        "base_url": "https://api.deepseek.com/v1",
        "provider_type": "single",
        "default_model": "deepseek-chat",
        "models_api": None,
        "auth_type": "bearer"
    },
    "baidu": {
        "name": "百度千帆",
        "base_url": "https://aip.baidubce.com/rpc/2.0/ai_custom/v1/wenxinworkshop",
        "provider_type": "coding_plan",
        "default_model": None,
        "models_api": "/models",
        "auth_type": "access_token"
    },
    "volcengine": {
        "name": "火山方舟",
        "base_url": "https://ark.cn-beijing.volces.com/api/v3",
        "provider_type": "coding_plan",
        "default_model": None,
        "models_api": "/models",
        "auth_type": "bearer"
    },
    "unicom": {
        "name": "联通云",
        "base_url": "",  # 待确认
        "provider_type": "coding_plan",
        "default_model": None,
        "models_api": "/v1/models",
        "auth_type": "bearer"
    },
    "custom": {
        "name": "自定义",
        "base_url": "",
        "provider_type": "single",
        "default_model": "",
        "models_api": "/v1/models",
        "auth_type": "bearer"
    }
}


def get_provider_config(provider: str) -> ProviderConfig | None:
    """获取提供商配置

    Args:
        provider: 提供商标识

    Returns:
        提供商配置，不存在则返回 None
    """
    return PRESET_PROVIDERS.get(provider)


def get_all_providers() -> list[dict]:
    """获取所有预设提供商列表

    Returns:
        提供商列表，包含 id 和 name
    """
    return [
        {"id": key, "name": config["name"]}
        for key, config in PRESET_PROVIDERS.items()
    ]
```

- [ ] **Step 2: 提交**

```bash
git add backend/app/services/model_providers.py
git commit -m "feat(backend): add preset model providers configuration

- Support DeepSeek, Baidu, Volcengine, Unicom, Custom
- Define provider_type and models_api for each

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

### Task 7: 更新 Pydantic Schemas

**Files:**
- Modify: `backend/app/schemas/model_config.py`

- [ ] **Step 1: 更新 schema 文件**

```python
# backend/app/schemas/model_config.py
"""模型配置 Pydantic Schemas"""

from datetime import datetime
from typing import Optional
from pydantic import BaseModel


class ModelItem(BaseModel):
    """Coding Plan 中的单个模型"""
    id: str
    name: str
    is_enabled: bool = True
    health_status: Optional[str] = None


class ModelConfigBase(BaseModel):
    """模型配置基础模型"""
    name: str
    provider: str
    provider_type: str = "single"
    base_url: str
    model_name: Optional[str] = None
    models: Optional[list[ModelItem]] = None
    api_key: Optional[str] = None


class ModelConfigCreate(ModelConfigBase):
    """创建模型配置请求"""
    pass


class ModelConfigUpdate(BaseModel):
    """更新模型配置请求"""
    name: Optional[str] = None
    base_url: Optional[str] = None
    model_name: Optional[str] = None
    models: Optional[list[ModelItem]] = None
    is_enabled: Optional[bool] = None
    api_key: Optional[str] = None
    clear_api_key: bool = False


class ModelConfigResponse(BaseModel):
    """模型配置响应"""
    id: int
    name: str
    provider: str
    provider_type: str = "single"
    base_url: str
    model_name: Optional[str] = None
    models: Optional[list[ModelItem]] = None
    has_api_key: bool
    is_enabled: bool
    is_default: bool
    health_status: Optional[str] = None
    health_latency: Optional[int] = None
    last_health_check: Optional[datetime] = None
    created_at: datetime
    updated_at: datetime

    class Config:
        from_attributes = True


class ModelConfigListResponse(BaseModel):
    """模型配置列表响应"""
    models: list[ModelConfigResponse]


class HealthCheckResponse(BaseModel):
    """健康检查响应"""
    status: str  # "healthy" | "unhealthy"
    latency: Optional[int] = None
    error: Optional[str] = None


class FetchModelsRequest(BaseModel):
    """获取模型列表请求"""
    provider: str
    base_url: str
    api_key: str


class FetchModelsResponse(BaseModel):
    """获取模型列表响应"""
    models: list[dict]  # [{"id": str, "name": str}]
    error: Optional[str] = None
    allow_manual: bool = False


class ProviderInfo(BaseModel):
    """提供商信息"""
    id: str
    name: str
    provider_type: str
    base_url: str


class ProvidersListResponse(BaseModel):
    """提供商列表响应"""
    providers: list[ProviderInfo]
```

- [ ] **Step 2: 提交**

```bash
git add backend/app/schemas/model_config.py
git commit -m "feat(schemas): add provider_type and models to ModelConfig schemas

- Add ModelItem for coding_plan models
- Add FetchModelsRequest/Response for API
- Add ProviderInfo for frontend dropdown

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

### Task 8: 添加 fetch-models API 端点

**Files:**
- Modify: `backend/app/api/model_configs.py`

- [ ] **Step 1: 添加导入**

在 `backend/app/api/model_configs.py` 顶部添加：

```python
import httpx
from app.services.model_providers import get_provider_config, PRESET_PROVIDERS
from app.schemas.model_config import (
    ModelConfigCreate,
    ModelConfigUpdate,
    ModelConfigResponse,
    ModelConfigListResponse,
    HealthCheckResponse,
    FetchModelsRequest,
    FetchModelsResponse,
    ProviderInfo,
    ProvidersListResponse,
)
```

- [ ] **Step 2: 添加获取提供商列表端点**

在文件中添加：

```python
@router.get("/providers", response_model=ProvidersListResponse)
async def list_providers():
    """获取所有预设提供商列表"""
    providers = [
        ProviderInfo(
            id=key,
            name=config["name"],
            provider_type=config["provider_type"],
            base_url=config["base_url"]
        )
        for key, config in PRESET_PROVIDERS.items()
    ]
    return ProvidersListResponse(providers=providers)
```

- [ ] **Step 3: 添加获取模型列表端点**

```python
@router.post("/fetch-models", response_model=FetchModelsResponse)
async def fetch_available_models(
    request: FetchModelsRequest,
    current_user: User = Depends(get_current_user)
):
    """从 Coding Plan API 获取可用模型列表

    尝试调用 API 的 /models 端点获取模型列表。
    如果 API 不支持，返回错误提示用户手动输入。
    """
    provider_config = get_provider_config(request.provider)

    if not provider_config:
        return FetchModelsResponse(
            models=[],
            error=f"未知的提供商: {request.provider}",
            allow_manual=True
        )

    if provider_config["provider_type"] != "coding_plan":
        return FetchModelsResponse(
            models=[],
            error="该提供商不是 Coding Plan 类型",
            allow_manual=False
        )

    models_api = provider_config.get("models_api", "/v1/models")

    try:
        async with httpx.AsyncClient(timeout=30.0) as client:
            # 尝试调用 /models 端点
            response = await client.get(
                f"{request.base_url.rstrip('/')}{models_api}",
                headers={
                    "Authorization": f"Bearer {request.api_key}"
                }
            )

            if response.status_code == 200:
                data = response.json()
                # 不同 API 返回格式可能不同，尝试解析
                models = []
                if isinstance(data, list):
                    models = [{"id": m.get("id", m.get("name")), "name": m.get("id", m.get("name"))} for m in data]
                elif isinstance(data, dict) and "data" in data:
                    models = [{"id": m.get("id", m.get("name")), "name": m.get("id", m.get("name"))} for m in data["data"]]
                elif isinstance(data, dict) and "models" in data:
                    models = [{"id": m.get("id", m.get("name")), "name": m.get("id", m.get("name"))} for m in data["models"]]

                return FetchModelsResponse(models=models)

            return FetchModelsResponse(
                models=[],
                error=f"API 返回错误: {response.status_code}",
                allow_manual=True
            )

    except httpx.TimeoutException:
        return FetchModelsResponse(
            models=[],
            error="请求超时，请检查 API 地址是否正确",
            allow_manual=True
        )
    except Exception as e:
        return FetchModelsResponse(
            models=[],
            error=f"获取模型列表失败: {str(e)}",
            allow_manual=True
        )
```

- [ ] **Step 4: 更新创建端点支持新字段**

修改 `build_config_response` 函数：

```python
def build_config_response(c: ModelConfig) -> ModelConfigResponse:
    """构建模型配置响应"""
    return ModelConfigResponse(
        id=c.id,
        name=c.name,
        provider=c.provider,
        provider_type=c.provider_type or "single",
        base_url=c.base_url,
        model_name=c.model_name,
        models=c.models,
        has_api_key=bool(c.api_key_encrypted),
        is_enabled=c.is_enabled,
        is_default=c.is_default,
        health_status=c.health_status,
        health_latency=c.health_latency,
        last_health_check=c.last_health_check,
        created_at=c.created_at,
        updated_at=c.updated_at,
    )
```

修改 `create_model_config` 端点：

```python
@router.post("/", response_model=ModelConfigResponse)
async def create_model_config(
    request: ModelConfigCreate,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user)
):
    """创建新的模型配置"""
    config = ModelConfig(
        user_id=current_user.id,
        name=request.name,
        provider=request.provider,
        provider_type=request.provider_type or "single",
        base_url=request.base_url,
        model_name=request.model_name,
        models=[m.model_dump() for m in request.models] if request.models else None,
        is_enabled=True,
        is_default=False,
        health_status="unknown",
    )

    if request.api_key:
        config.api_key_encrypted = encrypt_api_key(request.api_key, current_user.id)

    db.add(config)
    db.commit()
    db.refresh(config)

    return build_config_response(config)
```

- [ ] **Step 5: 提交**

```bash
git add backend/app/api/model_configs.py
git commit -m "feat(api): add fetch-models and providers endpoints

- GET /providers: list all preset providers
- POST /fetch-models: fetch models from coding plan API
- Update create endpoint to support new fields

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

## Phase 3: 模型配置前端

### Task 9: 更新前端类型定义

**Files:**
- Modify: `frontend/src/types/index.ts`

- [ ] **Step 1: 添加模型相关类型**

在 `frontend/src/types/index.ts` 中添加：

```typescript
// 模型配置相关类型
export interface ModelItem {
  id: string
  name: string
  is_enabled: boolean
  health_status?: string
}

export interface ModelConfig {
  id: number
  name: string
  provider: string
  provider_type: 'single' | 'coding_plan'
  base_url: string
  model_name?: string
  models?: ModelItem[]
  has_api_key: boolean
  is_enabled: boolean
  is_default: boolean
  health_status?: string
  health_latency?: number
  last_health_check?: string
  created_at: string
  updated_at: string
}

export interface ModelConfigListResponse {
  models: ModelConfig[]
}

export interface ProviderInfo {
  id: string
  name: string
  provider_type: 'single' | 'coding_plan'
  base_url: string
}

export interface ProvidersListResponse {
  providers: ProviderInfo[]
}

export interface FetchModelsResponse {
  models: { id: string; name: string }[]
  error?: string
  allow_manual: boolean
}
```

- [ ] **Step 2: 提交**

```bash
git add frontend/src/types/index.ts
git commit -m "feat(frontend): add ModelConfig and Provider types

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

### Task 10: 更新 API 客户端

**Files:**
- Modify: `frontend/src/lib/api.ts`

- [ ] **Step 1: 添加模型配置 API 方法**

在 `frontend/src/lib/api.ts` 中添加：

```typescript
// 模型配置 API
export const modelConfigsApi = {
  list: async (): Promise<ModelConfigListResponse> => {
    const response = await fetch('/api/model_configs/', {
      headers: getAuthHeaders(),
    })
    if (!response.ok) throw new Error('获取模型配置失败')
    return response.json()
  },

  getProviders: async (): Promise<ProvidersListResponse> => {
    const response = await fetch('/api/model_configs/providers', {
      headers: getAuthHeaders(),
    })
    if (!response.ok) throw new Error('获取提供商列表失败')
    return response.json()
  },

  fetchModels: async (data: {
    provider: string
    base_url: string
    api_key: string
  }): Promise<FetchModelsResponse> => {
    const response = await fetch('/api/model_configs/fetch-models', {
      method: 'POST',
      headers: {
        ...getAuthHeaders(),
        'Content-Type': 'application/json',
      },
      body: JSON.stringify(data),
    })
    if (!response.ok) throw new Error('获取模型列表失败')
    return response.json()
  },

  create: async (data: {
    name: string
    provider: string
    provider_type: string
    base_url: string
    model_name?: string
    models?: ModelItem[]
    api_key?: string
  }): Promise<ModelConfig> => {
    const response = await fetch('/api/model_configs/', {
      method: 'POST',
      headers: {
        ...getAuthHeaders(),
        'Content-Type': 'application/json',
      },
      body: JSON.stringify(data),
    })
    if (!response.ok) throw new Error('创建模型配置失败')
    return response.json()
  },

  update: async (id: number, data: Partial<{
    name: string
    base_url: string
    model_name: string
    models: ModelItem[]
    is_enabled: boolean
    api_key: string
    clear_api_key: boolean
  }>): Promise<ModelConfig> => {
    const response = await fetch(`/api/model_configs/${id}`, {
      method: 'PUT',
      headers: {
        ...getAuthHeaders(),
        'Content-Type': 'application/json',
      },
      body: JSON.stringify(data),
    })
    if (!response.ok) throw new Error('更新模型配置失败')
    return response.json()
  },

  delete: async (id: number): Promise<void> => {
    const response = await fetch(`/api/model_configs/${id}`, {
      method: 'DELETE',
      headers: getAuthHeaders(),
    })
    if (!response.ok) throw new Error('删除模型配置失败')
  },

  setDefault: async (id: number): Promise<ModelConfig> => {
    const response = await fetch(`/api/model_configs/${id}/default`, {
      method: 'PUT',
      headers: getAuthHeaders(),
    })
    if (!response.ok) throw new Error('设置默认模型失败')
    return response.json()
  },

  checkHealth: async (id: number): Promise<{ status: string; latency?: number; error?: string }> => {
    const response = await fetch(`/api/model_configs/${id}/health`, {
      method: 'POST',
      headers: getAuthHeaders(),
    })
    if (!response.ok) throw new Error('健康检查失败')
    return response.json()
  },
}
```

- [ ] **Step 2: 提交**

```bash
git add frontend/src/lib/api.ts
git commit -m "feat(frontend): add modelConfigsApi methods

- list, getProviders, fetchModels
- create, update, delete
- setDefault, checkHealth

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

### Task 11: 创建 ModelConfigItem 组件

**Files:**
- Create: `frontend/src/components/settings/ModelConfigItem.tsx`

- [ ] **Step 1: 创建组件文件**

```tsx
// frontend/src/components/settings/ModelConfigItem.tsx
import { useState } from 'react'
import { ChevronDown, Star, Trash2, RefreshCw } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { cn } from '@/lib/utils'
import type { ModelConfig } from '@/types'

interface ModelConfigItemProps {
  config: ModelConfig
  onSetDefault?: (id: number) => void
  onDelete?: (id: number) => void
  onRefresh?: (id: number) => void
}

export default function ModelConfigItem({
  config,
  onSetDefault,
  onDelete,
  onRefresh,
}: ModelConfigItemProps) {
  const [expanded, setExpanded] = useState(false)

  // 健康状态颜色
  const getHealthColor = (status?: string) => {
    switch (status) {
      case 'healthy':
        return 'text-green-600'
      case 'unhealthy':
        return 'text-red-600'
      default:
        return 'text-gray-400'
    }
  }

  // 健康状态文本
  const getHealthText = (status?: string) => {
    switch (status) {
      case 'healthy':
        return '健康'
      case 'unhealthy':
        return '异常'
      default:
        return '未知'
    }
  }

  // 单模型配置
  if (config.provider_type === 'single') {
    return (
      <div className="flex items-center p-3 border rounded-lg hover:bg-muted/50 transition-colors">
        {/* 默认标记 */}
        {config.is_default ? (
          <Star className="h-4 w-4 text-yellow-500 mr-2 fill-yellow-500" />
        ) : (
          <button
            onClick={() => onSetDefault?.(config.id)}
            className="h-4 w-4 mr-2 text-gray-300 hover:text-yellow-500 transition-colors"
            title="设为默认"
          >
            <Star className="h-4 w-4" />
          </button>
        )}

        {/* 名称 */}
        <span className="font-medium flex-1">{config.name}</span>

        {/* 健康状态 */}
        <span className={cn('text-sm mr-3', getHealthColor(config.health_status))}>
          {getHealthText(config.health_status)}
          {config.health_latency && ` · ${config.health_latency}ms`}
        </span>

        {/* 操作按钮 */}
        <Button
          variant="ghost"
          size="sm"
          onClick={() => onRefresh?.(config.id)}
          title="健康检查"
        >
          <RefreshCw className="h-4 w-4" />
        </Button>
        <Button
          variant="ghost"
          size="sm"
          onClick={() => onDelete?.(config.id)}
          title="删除"
        >
          <Trash2 className="h-4 w-4" />
        </Button>
      </div>
    )
  }

  // Coding Plan 配置（可展开）
  const enabledCount = config.models?.filter(m => m.is_enabled).length || 0

  return (
    <div className="border rounded-lg overflow-hidden">
      {/* 标题行 */}
      <div
        className="flex items-center p-3 cursor-pointer hover:bg-muted/50 transition-colors"
        onClick={() => setExpanded(!expanded)}
      >
        <ChevronDown
          className={cn('h-4 w-4 mr-2 transition-transform', expanded && 'rotate-180')}
        />

        {/* 默认标记 */}
        {config.is_default ? (
          <Star className="h-4 w-4 text-yellow-500 mr-2 fill-yellow-500" />
        ) : (
          <button
            onClick={(e) => {
              e.stopPropagation()
              onSetDefault?.(config.id)
            }}
            className="h-4 w-4 mr-2 text-gray-300 hover:text-yellow-500 transition-colors"
            title="设为默认"
          >
            <Star className="h-4 w-4" />
          </button>
        )}

        {/* 名称 */}
        <span className="font-medium flex-1">{config.name}</span>

        {/* 模型数量 */}
        <span className="text-muted-foreground text-sm mr-3">
          {enabledCount} 个模型
        </span>
      </div>

      {/* 展开的模型列表 */}
      {expanded && config.models && (
        <div className="border-t bg-muted/30 p-3 pl-10 space-y-2">
          {config.models.map((model) => (
            <div key={model.id} className="flex items-center text-sm">
              <span className={cn('flex-1', !model.is_enabled && 'text-muted-foreground')}>
                {model.name}
              </span>
              <span className={cn('mr-3', getHealthColor(model.health_status))}>
                {getHealthText(model.health_status)}
              </span>
            </div>
          ))}
        </div>
      )}
    </div>
  )
}
```

- [ ] **Step 2: 提交**

```bash
git add frontend/src/components/settings/ModelConfigItem.tsx
git commit -m "feat(frontend): add ModelConfigItem component

- Support single and coding_plan display modes
- Expandable list for coding_plan models
- Health status indicators

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

### Task 12: 创建 AddModelDialog 组件

**Files:**
- Create: `frontend/src/components/settings/AddModelDialog.tsx`

- [ ] **Step 1: 创建对话框组件**

```tsx
// frontend/src/components/settings/AddModelDialog.tsx
import { useState, useEffect } from 'react'
import { toast } from 'sonner'
import {
  Dialog,
  DialogContent,
  DialogHeader,
  DialogTitle,
  DialogFooter,
} from '@/components/ui/dialog'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from '@/components/ui/select'
import { Checkbox } from '@/components/ui/checkbox'
import { modelConfigsApi } from '@/lib/api'
import type { ProviderInfo, ModelItem } from '@/types'

interface AddModelDialogProps {
  open: boolean
  onOpenChange: (open: boolean) => void
  onSuccess: () => void
}

export default function AddModelDialog({
  open,
  onOpenChange,
  onSuccess,
}: AddModelDialogProps) {
  const [providers, setProviders] = useState<ProviderInfo[]>([])
  const [provider, setProvider] = useState('deepseek')
  const [name, setName] = useState('')
  const [baseUrl, setBaseUrl] = useState('')
  const [apiKey, setApiKey] = useState('')
  const [modelName, setModelName] = useState('')
  const [availableModels, setAvailableModels] = useState<{ id: string; name: string }[]>([])
  const [selectedModels, setSelectedModels] = useState<string[]>([])
  const [loading, setLoading] = useState(false)
  const [fetchingModels, setFetchingModels] = useState(false)

  // 加载提供商列表
  useEffect(() => {
    async function loadProviders() {
      try {
        const result = await modelConfigsApi.getProviders()
        setProviders(result.providers)
      } catch (err) {
        console.error('Failed to load providers:', err)
      }
    }
    if (open) {
      loadProviders()
    }
  }, [open])

  // 监听 provider 变化，自动填充配置
  useEffect(() => {
    const selectedProvider = providers.find(p => p.id === provider)
    if (selectedProvider && provider !== 'custom') {
      setBaseUrl(selectedProvider.base_url)
      setName(selectedProvider.name)
      // 自动填充默认模型名称
      if (selectedProvider.provider_type === 'single') {
        setModelName('deepseek-chat') // DeepSeek 默认模型
      }
    }
    // 重置模型列表
    setAvailableModels([])
    setSelectedModels([])
  }, [provider, providers])

  // 获取当前提供商类型
  const providerType = providers.find(p => p.id === provider)?.provider_type || 'single'

  // 获取可用模型
  const handleFetchModels = async () => {
    if (!apiKey) {
      toast.error('请先输入 API Key')
      return
    }

    setFetchingModels(true)
    try {
      const result = await modelConfigsApi.fetchModels({
        provider,
        base_url: baseUrl,
        api_key: apiKey,
      })

      if (result.error) {
        toast.error(result.error)
        // 如果允许手动输入，显示空列表
        if (result.allow_manual) {
          setAvailableModels([])
        }
      } else {
        setAvailableModels(result.models)
        toast.success(`获取到 ${result.models.length} 个可用模型`)
      }
    } catch (err) {
      toast.error('获取模型列表失败')
    } finally {
      setFetchingModels(false)
    }
  }

  // 切换模型选择
  const toggleModel = (modelId: string) => {
    setSelectedModels(prev =>
      prev.includes(modelId)
        ? prev.filter(id => id !== modelId)
        : [...prev, modelId]
    )
  }

  // 保存配置
  const handleSave = async () => {
    if (!name.trim()) {
      toast.error('请输入显示名称')
      return
    }
    if (!baseUrl.trim()) {
      toast.error('请输入 API 地址')
      return
    }
    if (!apiKey.trim()) {
      toast.error('请输入 API Key')
      return
    }

    // 单模型验证
    if (providerType === 'single' && !modelName.trim()) {
      toast.error('请输入模型名称')
      return
    }

    // Coding Plan 验证
    if (providerType === 'coding_plan' && selectedModels.length === 0) {
      toast.error('请至少选择一个模型')
      return
    }

    setLoading(true)
    try {
      // 构建 models 数组
      const models: ModelItem[] | undefined = providerType === 'coding_plan'
        ? availableModels
            .filter(m => selectedModels.includes(m.id))
            .map(m => ({
              id: m.id,
              name: m.name,
              is_enabled: true,
            }))
        : undefined

      await modelConfigsApi.create({
        name,
        provider,
        provider_type: providerType,
        base_url: baseUrl,
        model_name: providerType === 'single' ? modelName : undefined,
        models,
        api_key: apiKey,
      })

      toast.success('模型配置已保存')
      onSuccess()
      onOpenChange(false)

      // 重置表单
      setName('')
      setApiKey('')
      setModelName('')
      setAvailableModels([])
      setSelectedModels([])
    } catch (err) {
      toast.error(err instanceof Error ? err.message : '保存失败')
    } finally {
      setLoading(false)
    }
  }

  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent className="max-w-md">
        <DialogHeader>
          <DialogTitle>添加模型</DialogTitle>
        </DialogHeader>

        <div className="space-y-4 py-4">
          {/* 提供商选择 */}
          <div>
            <Label>提供商</Label>
            <Select value={provider} onValueChange={setProvider}>
              <SelectTrigger>
                <SelectValue />
              </SelectTrigger>
              <SelectContent>
                {providers.map(p => (
                  <SelectItem key={p.id} value={p.id}>
                    {p.name}
                  </SelectItem>
                ))}
              </SelectContent>
            </Select>
          </div>

          {/* 显示名称 */}
          <div>
            <Label>显示名称</Label>
            <Input
              value={name}
              onChange={e => setName(e.target.value)}
              placeholder="输入显示名称"
            />
          </div>

          {/* API 地址 */}
          <div>
            <Label>API 地址</Label>
            <Input
              value={baseUrl}
              onChange={e => setBaseUrl(e.target.value)}
              placeholder="https://api.example.com/v1"
              disabled={provider !== 'custom'}
            />
          </div>

          {/* API Key */}
          <div>
            <Label>API Key</Label>
            <Input
              type="password"
              value={apiKey}
              onChange={e => setApiKey(e.target.value)}
              placeholder="输入 API Key"
            />
          </div>

          {/* 单模型：模型名称输入 */}
          {providerType === 'single' && (
            <div>
              <Label>模型名称</Label>
              <Input
                value={modelName}
                onChange={e => setModelName(e.target.value)}
                placeholder="例如：deepseek-chat"
              />
            </div>
          )}

          {/* Coding Plan：获取模型按钮 */}
          {providerType === 'coding_plan' && (
            <Button
              variant="outline"
              onClick={handleFetchModels}
              disabled={!apiKey || fetchingModels}
              className="w-full"
            >
              {fetchingModels ? '获取中...' : '获取可用模型'}
            </Button>
          )}

          {/* Coding Plan：模型选择列表 */}
          {providerType === 'coding_plan' && availableModels.length > 0 && (
            <div>
              <Label>选择模型</Label>
              <div className="border rounded-lg divide-y max-h-48 overflow-y-auto">
                {availableModels.map(model => (
                  <label
                    key={model.id}
                    className="flex items-center p-2 hover:bg-muted/50 cursor-pointer"
                  >
                    <Checkbox
                      checked={selectedModels.includes(model.id)}
                      onCheckedChange={() => toggleModel(model.id)}
                    />
                    <span className="ml-2">{model.name}</span>
                  </label>
                ))}
              </div>
              <p className="text-xs text-muted-foreground mt-1">
                已选择 {selectedModels.length} 个模型
              </p>
            </div>
          )}
        </div>

        <DialogFooter>
          <Button variant="outline" onClick={() => onOpenChange(false)}>
            取消
          </Button>
          <Button onClick={handleSave} disabled={loading}>
            {loading ? '保存中...' : '保存'}
          </Button>
        </DialogFooter>
      </DialogContent>
    </Dialog>
  )
}
```

- [ ] **Step 2: 提交**

```bash
git add frontend/src/components/settings/AddModelDialog.tsx
git commit -m "feat(frontend): add AddModelDialog component

- Provider dropdown with auto-fill
- Single model and coding_plan modes
- Fetch models from API
- Multi-select for coding_plan

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

### Task 13: 更新 Settings 页面

**Files:**
- Modify: `frontend/src/pages/Settings.tsx`

- [ ] **Step 1: 重构模型配置部分**

找到模型配置相关代码，替换为：

```tsx
// frontend/src/pages/Settings.tsx
import { useState, useEffect } from 'react'
import { toast } from 'sonner'
import { Plus } from 'lucide-react'
import { Button } from '@/components/ui/button'
import ModelConfigItem from '@/components/settings/ModelConfigItem'
import AddModelDialog from '@/components/settings/AddModelDialog'
import { modelConfigsApi } from '@/lib/api'
import type { ModelConfig } from '@/types'

// ... 其他 Settings 页面代码 ...

export default function Settings() {
  const [modelConfigs, setModelConfigs] = useState<ModelConfig[]>([])
  const [showAddDialog, setShowAddDialog] = useState(false)
  const [loading, setLoading] = useState(true)

  // 加载模型配置
  const loadModelConfigs = async () => {
    try {
      const result = await modelConfigsApi.list()
      setModelConfigs(result.models)
    } catch (err) {
      toast.error('加载模型配置失败')
    } finally {
      setLoading(false)
    }
  }

  useEffect(() => {
    loadModelConfigs()
  }, [])

  // 设置默认模型
  const handleSetDefault = async (id: number) => {
    try {
      await modelConfigsApi.setDefault(id)
      await loadModelConfigs()
      toast.success('已设置为默认模型')
    } catch (err) {
      toast.error('设置默认模型失败')
    }
  }

  // 删除模型配置
  const handleDelete = async (id: number) => {
    try {
      await modelConfigsApi.delete(id)
      await loadModelConfigs()
      toast.success('模型配置已删除')
    } catch (err) {
      toast.error('删除失败')
    }
  }

  // 健康检查
  const handleRefresh = async (id: number) => {
    try {
      const result = await modelConfigsApi.checkHealth(id)
      await loadModelConfigs()
      if (result.status === 'healthy') {
        toast.success(`健康检查通过 · ${result.latency}ms`)
      } else {
        toast.error(`健康检查失败: ${result.error}`)
      }
    } catch (err) {
      toast.error('健康检查失败')
    }
  }

  return (
    <div className="space-y-8">
      {/* ... 其他设置部分 ... */}

      {/* 模型配置 */}
      <section>
        <div className="flex justify-between items-center mb-4">
          <h2 className="text-xl font-semibold">模型配置</h2>
          <Button onClick={() => setShowAddDialog(true)}>
            <Plus className="h-4 w-4 mr-2" />
            添加模型
          </Button>
        </div>

        {loading ? (
          <p className="text-muted-foreground">加载中...</p>
        ) : modelConfigs.length === 0 ? (
          <p className="text-muted-foreground">暂无模型配置，点击上方按钮添加</p>
        ) : (
          <div className="space-y-3">
            {modelConfigs.map(config => (
              <ModelConfigItem
                key={config.id}
                config={config}
                onSetDefault={handleSetDefault}
                onDelete={handleDelete}
                onRefresh={handleRefresh}
              />
            ))}
          </div>
        )}
      </section>

      {/* 添加模型对话框 */}
      <AddModelDialog
        open={showAddDialog}
        onOpenChange={setShowAddDialog}
        onSuccess={loadModelConfigs}
      />
    </div>
  )
}
```

- [ ] **Step 2: 重启前端验证**

```bash
docker compose restart frontend
```

在浏览器中验证：
1. 打开设置页面
2. 点击「添加模型」
3. 选择提供商，验证自动填充
4. 保存后验证列表显示

- [ ] **Step 3: 提交**

```bash
git add frontend/src/pages/Settings.tsx
git commit -m "refactor(frontend): update Settings page for multi-model

- Use ModelConfigItem for list display
- Add AddModelDialog integration
- Support set default, delete, refresh

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

## Phase 4: 灵感采集模型选择

### Task 14: 更新 InspirationForm 组件

**Files:**
- Modify: `frontend/src/components/project/InspirationForm.tsx`

- [ ] **Step 1: 添加模型选择状态和加载逻辑**

在 `InspirationForm.tsx` 中添加：

```tsx
import { modelConfigsApi } from '@/lib/api'
import type { ModelConfig } from '@/types'

interface InspirationFormProps {
  initialData?: Partial<InspirationData>
  onSubmit: (data: InspirationData, modelId?: number) => void  // 修改签名
}

export default function InspirationForm({ initialData, onSubmit }: InspirationFormProps) {
  // ... 现有状态 ...

  // 新增：模型选择状态
  const [availableModels, setAvailableModels] = useState<ModelConfig[]>([])
  const [selectedModelId, setSelectedModelId] = useState<number | null>(null)

  // 加载可用模型列表
  useEffect(() => {
    async function loadModels() {
      try {
        const result = await modelConfigsApi.list()
        const enabledModels = result.models.filter(m => m.is_enabled)
        setAvailableModels(enabledModels)

        // 默认选中用户的默认模型
        const defaultModel = enabledModels.find(m => m.is_default)
        if (defaultModel) {
          setSelectedModelId(defaultModel.id)
        }
      } catch (err) {
        console.error('Failed to load models:', err)
      }
    }
    loadModels()
  }, [])

  const handleSubmit = () => {
    // ... 现有验证逻辑 ...

    clearInspirationDraft()
    onSubmit(data, selectedModelId || undefined)  // 传递选中的模型 ID
  }

  // ... 其余代码 ...
```

- [ ] **Step 2: 在表单底部添加模型选择 UI**

在操作按钮之前添加：

```tsx
{/* 分隔线 */}
<div className="border-t border-gray-200" />

{/* 模型选择 */}
<div>
  <div className="text-sm font-medium text-gray-700 mb-2">
    生成模型
  </div>
  <select
    className="w-full h-11 px-3 rounded-lg border-2 border-gray-200 bg-white text-sm focus:border-blue-500 focus:outline-none"
    value={selectedModelId || ''}
    onChange={(e) => setSelectedModelId(Number(e.target.value) || null)}
  >
    {availableModels.length === 0 ? (
      <option value="">请先在设置中添加模型</option>
    ) : (
      availableModels.map(model => (
        <option key={model.id} value={model.id}>
          {model.name}{model.is_default ? '（默认）' : ''}
        </option>
      ))
    )}
  </select>
  <p className="text-xs text-gray-500 mt-1">
    选择用于生成内容的 AI 模型
  </p>
</div>
```

- [ ] **Step 3: 提交**

```bash
git add frontend/src/components/project/InspirationForm.tsx
git commit -m "feat(frontend): add model selection to InspirationForm

- Load available models on mount
- Select default model automatically
- Pass model ID to onSubmit callback

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

### Task 15: 更新工作流 API 支持模型 ID

**Files:**
- Modify: `frontend/src/components/project/OutlineWorkflow.tsx`
- Modify: `frontend/src/pages/ProjectDetail.tsx`
- Modify: `backend/app/api/workflow.py`
- Modify: `backend/app/schemas/workflow.py`

- [ ] **Step 1: 更新后端 workflow schema**

修改 `backend/app/schemas/workflow.py`：

```python
class WorkflowRunRequest(BaseModel):
    """工作流运行请求"""
    llm_config_id: Optional[int] = None  # 新增：指定模型配置 ID
```

- [ ] **Step 2: 更新后端 workflow API**

修改 `backend/app/api/workflow.py` 中的 `run_workflow` 端点：

```python
@router.post("/projects/{project_id}/workflow/run")
async def run_workflow(
    project_id: int,
    request: WorkflowRunRequest,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user)
):
    # ... 验证代码 ...

    initial_state: NovelState = {
        "project_id": project_id,
        "llm_config_id": request.llm_config_id,  # 传递模型 ID
        # ... 其他状态 ...
    }

    # ... 其余代码 ...
```

- [ ] **Step 3: 更新前端 workflowApi**

修改 `frontend/src/lib/workflowApi.ts`：

```typescript
runWorkflow: async (
  projectId: number,
  llmConfigId?: number,
  callbacks: WorkflowCallbacks
): Promise<void> => {
  // ... 现有代码 ...

  const body = {
    llm_config_id: llmConfigId,  // 新增参数
  }

  // ... 其余代码 ...
}
```

- [ ] **Step 4: 更新 OutlineWorkflow 传递模型 ID**

修改 `frontend/src/components/project/OutlineWorkflow.tsx`：

```tsx
// 接收 modelId 参数
interface OutlineWorkflowProps {
  projectId: number
  modelId?: number  // 新增
  // ... 其他 props
}

// 调用 workflowApi 时传递
await workflowApi.runWorkflow(projectId, modelId, callbacks)
```

- [ ] **Step 5: 更新 ProjectDetail 传递模型 ID**

修改 `frontend/src/pages/ProjectDetail.tsx`：

```tsx
// 在 OutlineWorkflow 组件使用处
<OutlineWorkflow
  projectId={project.id}
  modelId={selectedModelId}  // 传递选中的模型
  // ... 其他 props
/>
```

- [ ] **Step 6: 提交**

```bash
git add backend/app/schemas/workflow.py backend/app/api/workflow.py frontend/src/lib/workflowApi.ts frontend/src/components/project/OutlineWorkflow.tsx frontend/src/pages/ProjectDetail.tsx
git commit -m "feat: pass llm_config_id through workflow

- Add llm_config_id to WorkflowRunRequest
- Pass model selection from InspirationForm to workflow
- Backend uses specified model for generation

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

## Phase 5: 最终验证与提交

### Task 16: 运行完整测试并提交

- [ ] **Step 1: 运行后端测试**

```bash
docker exec novelagent-backend-1 pytest -v
```

- [ ] **Step 2: 运行前端测试**

```bash
cd frontend && npm run test:run
```

- [ ] **Step 3: 手动测试清单**

1. [ ] 章节生成后结尾无数字
2. [ ] Writing 页面按钮正确
3. [ ] 错误提示使用 Toast 而非 alert
4. [ ] 模型配置页面显示列表
5. [ ] 添加单模型配置成功
6. [ ] 添加 Coding Plan 配置成功
7. [ ] 灵感采集页模型选择正常
8. [ ] 使用指定模型生成大纲成功

- [ ] **Step 4: 更新 CHANGELOG**

```markdown
## v0.6.4 - 2026-04-24

### Features
- 支持多 AI 模型配置
- 支持 Coding Plan 类型 API（百度千帆、火山方舟、联通云）
- 灵感采集页新增模型选择

### Fixes
- 移除章节正文结尾的 LLM 生成数字
- Writing 页面按钮调整

### Improvements
- 使用 Toast 替换浏览器 alert 弹窗
```

- [ ] **Step 5: 最终提交**

```bash
git add -A
git commit -m "release: v0.6.4 multi-model support

Features:
- Multi-model configuration support
- Coding Plan API support (Baidu, Volcengine, Unicom)
- Model selection in Inspiration form

Fixes:
- Remove trailing numbers from chapter content
- Adjust Writing page buttons

Improvements:
- Replace alert() with Sonner toast

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

## 总结

本计划包含 **16 个任务**，分为 **5 个阶段**：

| 阶段 | 任务数 | 描述 |
|------|--------|------|
| Phase 1 | 3 | 快速优化：章节清理、按钮调整、Toast 替换 |
| Phase 2 | 5 | 模型配置后端：模型、迁移、配置、Schema、API |
| Phase 3 | 5 | 模型配置前端：类型、API 客户端、组件、页面 |
| Phase 4 | 2 | 灵感采集模型选择：表单、工作流 |
| Phase 5 | 1 | 最终验证与提交 |
